# 视觉方案工作台（VisionFlowPlatform）需求规格说明书

| 项目 | 内容 |
|------|------|
| 文档版本 | v2.0（重写版） |
| 编写日期 | 2026-09-14 |
| 产品定位 | 图形化机器视觉算法开发平台 |
| **对标基准** | **海康机器人 VisionMaster 4.4（VM 算法平台）** |
| 文档状态 | 现行有效 |

---

## 0. 修订说明

### 0.1 为什么重写

原始需求文件 `需求.txt` 存在**不可逆的编码损坏**：文件中包含 1520 个 `U+FFFD` 替换符（`EF BF BD` 字节序列）。

**损坏原因**：GBK 编码的中文内容被当作 UTF-8 解码，无法识别的字节被替换为 `U+FFFD` 并重新保存为 UTF-8。这属于**有损转换，原始字节已永久丢失，无法通过任何转码手段恢复**（转码只能改变"怎么看"，不能还原"已经被扔掉的字节"）。

同目录下的 `requirements_decoded.txt` 是早期一次失败的解码尝试产物，同样是乱码，已废弃。

因此本文档**不是对原文的简单转码**，而是基于以下三个来源**重建并全面升级**的需求基线：

1. 原始文件残留的**章节骨架与需求编号体系**（FR / NFR / DR / SR 编号得以保留，保证历史连续性）；
2. 代码库中**已实际实现的能力**（`include/`、`src/`、`src/NodeRegistry.cpp`）；
3. **VisionMaster 4.4** 公开的能力体系（定位 / 测量 / 识别 / 检测 / 深度学习 / 资源管理 / 3D）。

### 0.2 相对原始需求的主要变化

| 维度 | 原始需求（v1） | 本版（v2） |
|------|---------------|-----------|
| 覆盖范围 | 以 UI 交互为主（拖拽、连线、布局、删除） | 覆盖采集→预处理→定位→测量→识别→检测→深度学习→通讯→数据→平台全链路 |
| 对标 | 仅零散提及 VisionMaster | 全生命周期对标 VM 4.4，每条目标注对标模块 |
| 状态可见性 | 无 | 每条需求标注 ✅已实现 / ⚠️部分实现 / ❌未实现 |
| 非功能需求 | 仅性能/可靠性/稳定性 7 条 | 扩展为 7 大类 30+ 条（含安全性、可维护性、国际化） |
| 验收标准 | 无 | 新增可量化验收标准章节 |

---

### 0.3 当前成熟度声明（重要）

> **结论（2026-09-14 代码审查）**：平台已具备基本框架，但**执行引擎仍存在影响检测正确性与运行稳定性的问题，暂不宜宣称达到 VisionMaster 4.4 的工业交付水平**。
>
> 对标 VM 4.4 的前提是先补齐执行引擎的正确性、流程隔离与安全停止（详见 §3.21）。在此之前，文档中标注 ✅ 的条目指"功能存在 / 可打通 demo"，**不等同于已通过端到端工业场景验证**；所有 ✅ 最终需以 §7 验收标准的端到端场景支撑。

---

## 1. 产品概述

### 1.1 产品定位

一款**图形化、节点式（算子流）的机器视觉算法开发平台**。用户通过拖拽算子、连线构建视觉方案（Scheme），无需编写代码即可完成工业现场的**定位、测量、识别、检测**四大类任务。

### 1.2 目标用户

| 角色 | 特征 | 核心诉求 |
|------|------|---------|
| 视觉调试工程师 | 熟悉工艺，不写代码 | 拖拽即用、参数直观、结果可视 |
| 算法工程师 | 会 C++/Python | 可自定义算子、可脚本扩展 |
| 设备集成商 | 需对接 PLC/机器人 | 通讯矩阵完备、稳定 7×24 |
| 产线操作工 | 只操作不配置 | 运行界面简洁、权限受限 |

### 1.3 对标目标

**一句话目标**：在"定位 / 测量 / 识别 / 检测"四大场景上达到 VisionMaster 4.4 的**可用性与算子覆盖度**，并在**开放性**（脚本、SDK、自定义算子）与**自主可控**（OpenCV 主线）上形成差异化优势。

**分阶段目标**：

| 阶段 | 目标 | 衡量标准 |
|------|------|---------|
| 近期 | 补齐 VM 4.4 四大场景的核心算子与交互体验 | 覆盖 VM 4.4 常用算子 ≥ 80% |
| 中期 | 深度学习全栈 + 3D + 工业通讯矩阵 | 支持检测/分割/异常检测；支持 OPC UA、S7 |
| 长期 | 平台生态（SDK、算子市场、行业方案包） | 可提供二次开发 SDK 与算子插件规范 |

### 1.4 术语表

| 术语 | 说明 |
|------|------|
| 算子（Node / Tool） | 流程中最小的功能单元，如"模板匹配""卡尺测量" |
| 方案（Scheme / Solution） | 一个完整的视觉工程，存储为 `.vfp` 文件 |
| 流程（Flow） | 方案中的一个标签页，包含一组相互连线的算子 |
| 模块号 | 算子在流程中的序号，用于参数引用（如 `{1.角度}`） |
| ROI | 感兴趣区域，限定算法作用的图像范围 |
| Region / XLD | HALCON 中的区域 / 亚像素轮廓数据 |
| 配方（Recipe） | 同一方案下不同产品型号的参数集合 |
| 运行界面 | 面向操作工的简化界面，由 RuntimeInterfaceDesigner 设计 |
| 软触发 / 硬触发 | 由软件指令 / 由外部硬件信号触发采集 |

---

## 2. 系统范围与约束（SR）

| 编号 | 约束项 | 内容 | 状态 |
|------|--------|------|------|
| SR1 | 操作系统 | Windows 10/11 x64（工业现场主流） | ✅ |
| SR2 | 开发语言与框架 | C++17 + Qt 6（Widgets/Core/Gui/Sql/SerialPort/Network/SerialBus/Concurrent/Test） | ✅ |
| SR3 | 算法库 | HALCON 24.11 Progress（DeepOCR 等）、OpenCV 4.13.0（日常算法主线） | ✅ |
| SR4 | 相机 SDK | 海康 MVS（GigE/USB）、HALCON 采集接口（open_framegrabber） | ✅ |
| SR5 | 辅助库 | ZXing-C++（条码）、Tesseract+Leptonica（OCR） | ✅ |
| SR6 | 数据库 | SQLite（Qt6::Sql），每线程独立连接 | ✅ |
| SR7 | 构建 | CMake ≥ 3.16，MSVC，编译选项 `/utf-8` | ✅ |
| SR8 | 授权约束 | HALCON 为商业授权；**OpenCV 主线策略**降低对 HALCON 算子的依赖（见 `VisionIntegrationPolicy.h`） | ✅ |
| SR9 | 算法选型策略 | 日常算法一律优先 OpenCV；**HALCON 仅保留 DeepOCR** | ✅ |

---

## 3. 功能需求（FR）

> 优先级定义：**P0** 必须有（缺失则产品不可用）；**P1** 重要（影响竞争力）；**P2** 增强；**P3** 可选/长期。
> 状态定义：✅ 已实现　⚠️ 部分实现　❌ 未实现

### 3.1 流程编辑与拖拽交互（FR1.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR1.1 | 支持从工具箱拖拽算子到流程编辑区，拖拽过程有视觉反馈 | P0 | 工具箱拖拽 | ✅ |
| FR1.2 | 拖拽放置后算子自动进入编辑态，参数面板即时可用 | P0 | 模块属性 | ✅ |
| FR1.3 | 支持多流程标签页，每个流程独立执行器、可并发运行 | P1 | 多方案/多流程 | ✅ |
| FR1.4 | 算子支持重命名、模块号自动编号、注释图元 | P1 | 模块重命名/注释 | ✅ |
| FR1.5 | 支持节点复制（duplicate）、框选、对齐 | P1 | 批量操作 | ✅ |
| FR1.6 | 支持编辑锁定，防止误改已调试完成的流程 | P2 | 方案保护 | ✅ |
| FR1.7 | 支持算子搜索定位（Ctrl+F），按中英文名/别名/模块号搜索 | P1 | 算子搜索 | ⚠️ 部分（2026-09-24 逐处核对）：画布 Ctrl+F 按**节点名 + 模块号**定位、工具箱按**中文名 + 类型 id** 过滤，两条链路均可用；**英文名与别名不参与搜索匹配**（`englishName`/`aliases` 目前只服务 `NodeRegistry::findByName` 的类型解析），见 FR18.4 |
| FR1.8 | 支持参数查找（跨算子检索参数名） | P2 | 参数检索 | ✅ |
| FR1.9 | 支持算子分组（Group）/ 子流程封装，提升大规模流程可读性 | P1 | 流程分组 | ✅ 算子分组已交付（2026-09-23）；子流程封装属 FR15.10（设计期复用 + 运行期调用均已交付，2026-09-24） |

> FR1.9 实现口径：分组是**纯视觉容器**（`NodeGroupItem`）——不参与执行、不改节点/连线、不发图结构信号；
> 成员用**模块号**标识（跨存盘/撤销稳定），随方案保存（`extras` 的 `nodeGroups`）并随撤销快照回滚。
> 入口：画布空白右键「创建分组」/ 「编辑 → 创建分组」(Ctrl+G) / 「解散分组」(Ctrl+Shift+G)；
> 分组标题栏可拖动（组内其余区域仍可框选算子），右键改名，拖动成员时框体自动跟随。
> 选中分组框按 Delete = 解散（**只删框，不删算子**）。
> **Group 折叠（2026-09-23 已交付）**：双击标题栏（或右键「折叠/展开分组」、编辑菜单同项）把分组收成
> 一条标题栏，组内算子与其相关连线一并隐藏、框体宽度与位置不变（留作"这段有多宽"的线索），
> 折叠状态随方案保存并可撤销。可见性由 `FlowScene::refreshGroupVisibility` **统一重算**
> （连线两端分属不同分组、多组同时折叠时才不会算错；隐藏的算子会被取消选中，避免"删掉看不见的算子"）；
> 折叠是**纯显示**（真跑流程验证执行不受影响），且**解散分组时必定恢复成员可见性**
> （否则"解散"后算子会凭空消失）。编辑锁定（运行中）仍允许折叠/展开——它是查看动作。

### 3.2 连线与数据流（FR2.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR2.1 | 端口拖拽连线，支持图像/Region/数值/字符串/数组等类型 | P0 | 数据流连线 | ✅ |
| FR2.2 | 连线过程高亮显示**类型兼容**的端口，不兼容端口禁止连接 | P0 | 类型匹配 | ✅ |
| FR2.3 | 连线支持正交折线走线，避免穿过算子 | P1 | 连线美观 | ✅ |
| FR2.4 | 连线拖拽时提供视觉反馈（高亮/吸附） | P1 | 交互反馈 | ✅ |
| FR2.5 | 支持参数引用：在参数中填写 `{模块号.参数名}` 引用上游结果 | P1 | 数据引用 | ⚠️ 部分（**2026-09-25 由"✅ 已交付"降级**）：跨轮重新解析已交付并钉住——解析仅本轮生效、执行后还原表达式，`testParamRefResolvesEachRound` 真跑两轮通过 ⇒ 本条主诉求（引用不被下一轮沿用）成立。**未闭环**：还原点在所有 `emit` 之后、且 `try` 无 `catch(...)` ⇒ 该窗口内保存仍会把引用落成常量，非标准异常时参数永久停在常量上；详见 §3.21 E1「残留」 |
| FR2.6 | 支持连线删除、批量断开 | P1 | 连线管理 | ✅ |
| FR2.7 | 支持"一输出对多输入"广播，以及多输入汇总 | P1 | 数据分发 | ⚠️ 部分 |
| FR2.8 | 连线支持标签显示数据值（调试态） | P2 | 数据流调试 | ❌ |

### 3.3 项目管理与配方（FR3.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR3.1 | 方案保存/加载，使用 `.vfp` 后缀、JSON 格式存储 | P0 | 方案文件 | ✅ |
| FR3.2 | 方案文件自描述（含 schema 版本、节点 typeId），支持向后兼容升级 | P0 | 版本兼容 | ✅ |
| FR3.3 | 支持"另存为"、最近打开列表 | P1 | 文件管理 | ✅ |
| FR3.4 | 配方（Recipe）管理：同一方案下多产品型号参数切换、导入导出 | P1 | 产品配置 | ✅ |
| FR3.5 | 支持流程级变量（Int/Bool/Float/String）与全局变量 | P1 | 全局变量 | ✅ |
| FR3.6 | 方案支持加密/只读导出，保护知识产权 | P2 | 方案保护 | ✅ 已交付（2026-09-24）：`SchemeCrypto`（AES-256-CTR + PBKDF2-HMAC-SHA256，**自包含零第三方依赖**）+ `SchemePackage` 信封（版本/只读标志/盐/IV/密文/HMAC，老明文 `.vfp` 自动识别）+ 文件菜单"导出加密/只读方案…" + 打开加密方案弹口令 + 只读方案禁用覆盖保存（可另存为新方案）；`SchemePackageTest` 17 条（含 FIPS 197 与 NIST SP 800-38A 已知答案向量、错误口令拒绝、篡改拒绝）。已知限制：只读仅阻断"覆盖原分发文件"，未锁画布编辑（v1 取舍）；非加密只读包无 MAC |
| FR3.7 | 支持方案模板与行业方案包（一键套用） | P2 | 方案模板 | ❌ |

### 3.4 界面布局与运行界面（FR4.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR4.1 | 主界面分区：工具箱 / 流程编辑区 / 图像显示区 / 参数面板 / 日志区，比例合理（编辑区约占 1/3） | P0 | 主界面布局 | ✅ |
| FR4.2 | 各面板支持拖拽停靠、浮动、隐藏、布局保存与恢复 | P1 | 布局自定义 | ✅ |
| FR4.3 | 图像显示区支持多标签：全屏显示所有图像窗口，或标签页仅显示当前选中 | P0 | 图像窗口管理 | ✅ |
| FR4.4 | 品牌 Logo 展示（工具箱顶部/标题栏） | P2 | 品牌区 | ✅ |
| FR4.5 | 深色工业风主题，高对比连线与网格画布 | P1 | 视觉风格 | ✅ |
| FR4.6 | **运行界面设计器**：自定义面向操作工的简化界面（按钮/图像/结果/OK-NG 指示灯） | P1 | 运行界面（VM 4.4） | ✅ |
| FR4.7 | 运行界面布局持久化（runtime_interface.json），支持一键切换编辑/运行模式 | P1 | 运行态切换 | ✅ |
| FR4.8 | 支持双屏：编辑屏 + 运行屏分离 | P2 | 双屏运行 | ❌ |

### 3.5 节点与连线管理（FR5.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR5.1 | 支持通过右键菜单删除选中算子 | P0 | 删除模块 | ✅ |
| FR5.2 | 支持通过右键菜单删除选中连线 | P0 | 删除连线 | ✅ |
| FR5.3 | 删除算子时同步清理其关联连线，不留悬挂引用 | P0 | 联动清理 | ✅ |
| FR5.4 | 支持 Delete 快捷键删除 | P1 | 快捷操作 | ✅ |
| FR5.5 | 撤销/重做（快照式，深度 50），覆盖增删改与连线操作 | P0 | 撤销重做 | ✅ |
| FR5.6 | 算子启用/禁用（旁路），便于调试时跳过某环节 | P1 | 模块使能 | ❌ |

### 3.6 算子库全景与优先级（FR6 清单）

> 优先级：P0 核心必选；P1 常用；P2 进阶；P3 长尾。

| 优先级 | 算子 | VM 4.4 对应模块 | 实现路径 | 状态 |
|--------|------|-----------------|---------|------|
| P0 | 读图（本地/目录序列） | 图像源 | HALCON `read_image` | ✅ |
| P0 | 相机采集（海康 MVS） | 相机管理 | MVS SDK | ✅ |
| P0 | 相机采集（HALCON 接口） | 相机管理 | `open_framegrabber` | ✅ |
| P0 | 色彩转换 / 通道分离 | 图像处理 | `trans_from_rgb` / OpenCV | ✅ |
| P1 | 模糊（均值/高斯/中值） | 图像滤波 | OpenCV / HALCON | ✅ |
| P1 | 阈值分割（固定/OTSU/自适应） | 图像处理 | OpenCV | ✅ |
| P1 | 形态学（膨胀/腐蚀/开/闭/顶帽） | 图像处理 | OpenCV | ✅ |
| P1 | Blob 分析（连通域+特征筛选） | 检测 | OpenCV | ✅ |
| P1 | 边缘提取（亚像素） | 定位/测量 | OpenCV / HALCON | ✅ |
| P1 | 图像运算（加减乘除/求反） | 图像处理 | OpenCV | ✅ |
| P1 | ROI 裁剪 / 仿射变换（旋转/缩放/镜像/平移） | 图像处理 | OpenCV | ✅ |
| P1 | 直方图均衡 / 对比度拉伸 / 锐化 | 图像增强 | OpenCV | ✅ |
| P2 | 模板匹配（NCC / Shape） | **定位** | OpenCV TemplateMatch | ✅ |
| P2 | 特征匹配（点/形状） | **定位** | — | ❌ |
| P2 | 直线拟合 / 圆拟合 | **定位** | OpenCV FitLine/FitCircle | ✅ |
| P2 | 角点检测 | **定位** | HALCON CornerNode | ✅ |
| P2 | 位置修正 | **定位** | PositionCorrect | ✅ |
| P2 | 卡尺（Measure Pos/Pairs） | **测量** | OpenCV Caliper | ✅ |
| P2 | 距离测量（点点/点线/点圆） | **测量** | DistanceMeasure 系列 | ✅ |
| P2 | 角度测量 | **测量** | ⚠️ 源码保留未注册 | ⚠️ |
| P2 | 面积测量 | **测量** | OpencvBlob 面积端口替代 | ⚠️ |
| P2 | 相机标定 | **测量/标定** | OpenCV `calibrateCamera` | ✅ |
| P2 | 手眼标定 | **定位/标定** | HandEyeCalib | ✅ |
| P2 | N 点标定 | **标定** | NPointCalib | ✅ |
| P1 | 一维码识别 | **识别** | ZXing-C++ | ✅ |
| P1 | 二维码识别（QR/DM） | **识别** | OpenCV / ZXing | ✅ |
| P1 | OCR（传统） | **识别** | Tesseract | ✅ |
| P1 | OCR（深度学习） | **识别** | HALCON DeepOCR | ✅ |
| P2 | 缺陷检测（表面划痕/斑点） | **检测** | OpencvDefect | ✅ |
| P2 | 标准件比对 | **检测** | — | ❌ |
| P3 | 深度学习分类 | **深度学习** | OpenCV DNN(ONNX) / ANN_MLP | ✅ |
| P3 | 深度学习目标检测 | **深度学习** | — | ❌ |
| P3 | 深度学习分割 | **深度学习** | — | ❌ |
| P3 | 异常检测（仅 OK 样本建模） | **深度学习** | — | ❌ |
| P3 | 字符定位+识别（DL） | **深度学习** | — | ❌ |
| P3 | 图像检索 | **深度学习** | — | ❌ |
| P1 | 条件分支 If-Else | 逻辑 | ConditionalNode | ✅ |
| P1 | 循环 | 逻辑 | LoopNode | ✅ |
| P1 | 公式计算 | 逻辑 | FormulaNode | ✅ |
| P1 | 延时 / 计数 / 筛选 / 分类判定 / 排序 | 逻辑 | 各对应节点 | ✅ |
| P1 | 脚本（Python/Lua） | 扩展 | ScriptNode（带安全策略） | ✅ |
| P1 | 结果显示 / 写文件 / 数据记录 | 输出 | DisplaySink/WriteFile/Record | ✅ |
| P1 | 格式化 / 协议解析 | 输出 | Format/ProtocolParse | ✅ |
| P1 | TCP 客户端/服务器 | 通讯 | TcpCommNode | ✅ |
| P1 | 串口 | 通讯 | SerialCommNode | ✅ |
| P1 | Modbus TCP/RTU 主站+从站 | 通讯 | ModbusNode | ✅ |
| P1 | PLC（Modbus TCP，S7-1200/1500 等） | 通讯 | PlcCommNode | ✅ |
| P2 | OPC UA | 通讯 | — | ❌ |
| P2 | 原生 S7Comm / EtherCAT / Profinet | 通讯 | — | ❌ |
| P2 | 数字 IO 板卡 / 光源控制器 | 资源管理 | — | ❌ |
| P3 | 3D：点云预处理 / 轮廓匹配 / 特征提取 / 图像拼接 | 3D | — | ❌ |

### 3.7 图像采集（FR7.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR7.1 | 支持本地图片/目录序列读取，作为离线调试图像源 | P0 | 图像源 | ✅ |
| FR7.2 | 支持海康 MVS SDK 相机取流（GigE / USB） | P0 | 相机管理 | ✅ |
| FR7.3 | 支持 HALCON 采集接口（覆盖 GenICam/GigE Vision/USB3 多品牌） | P0 | 相机管理 | ✅ |
| FR7.4 | **多相机管理**：全局相机命名、多设备绑定、同一相机被多算子共享、冲突检测 | P0 | 多相机 | ✅ |
| FR7.5 | 相机参数配置：曝光、增益、帧率、ROI、像素格式、触发模式/触发源 | P0 | 相机参数 | ✅ |
| FR7.6 | 支持软触发、硬触发、自由运行三种采集模式 | P0 | 触发模式 | ✅ |
| FR7.7 | 相机掉线自动重连与状态提示 | P1 | 设备管理 | ⚠️ 部分 |
| FR7.8 | 支持更多品牌原生 SDK（Basler pylon、大华、大恒等） | P2 | 设备兼容 | ❌ |
| FR7.9 | 支持光源控制器亮度/通道控制 | P2 | **光源调节（VM 4.4）** | ❌ |

### 3.8 图像预处理与增强（FR8.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR8.1 | 色彩转换（RGB↔Gray↔HSV）、通道分离/合并 | P0 | 图像处理 | ✅ |
| FR8.2 | 滤波去噪：均值、高斯、中值、双边 | P0 | 图像滤波 | ✅ |
| FR8.3 | 对比度增强：直方图均衡、对比度拉伸、灰度拉伸、锐化 | P1 | 图像增强 | ✅ |
| FR8.4 | 形态学运算：膨胀、腐蚀、开、闭、顶帽、底帽 | P1 | 形态学 | ✅ |
| FR8.5 | 几何变换：平移、旋转、缩放、镜像、仿射、透视 | P1 | 图像变换 | ✅ |
| FR8.6 | 图像运算：加、减、乘、除、求反、位运算 | P1 | 图像运算 | ✅ |
| FR8.7 | ROI 管理：矩形/圆/椭圆/多边形/任意形状，支持掩膜编辑与 ROI 继承 | P0 | ROI 工具 | ✅ |
| FR8.8 | 频域处理（FFT 滤波） | P2 | 频域 | ❌ |
| FR8.9 | 图像拼接（大视野多图缝合） | P2 | **图像拼接（VM 4.4 3D）** | ❌ |

### 3.9 定位（FR9.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR9.1 | 模板匹配：支持平移、旋转、缩放，输出位置+角度+尺度+分数 | P0 | **定位** | ✅ |
| FR9.2 | 支持 NCC 灰度匹配与 Shape 形状匹配两种模式 | P1 | **定位** | ⚠️ 仅 OpenCV 版 |
| FR9.3 | 支持多目标匹配与结果排序 | P1 | **定位** | ⚠️ 部分 |
| FR9.4 | 直线/圆/椭圆拟合（亚像素） | P0 | **定位** | ✅ |
| FR9.5 | 角点 / 顶点 / 边缘位置检测 | P1 | **定位** | ✅ |
| FR9.6 | 斑点（Blob）位置检测 | P1 | **定位** | ✅ |
| FR9.7 | 位置修正：根据匹配结果将后续 ROI/坐标系对齐 | P1 | **定位** | ✅ |
| FR9.8 | 特征匹配（基于特征点的鲁棒匹配） | P2 | **定位** | ❌ |
| FR9.9 | 匹配结果支持亚像素精度与稳定性自评 | P2 | **定位** | ❌ |

### 3.10 几何测量（FR10.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR10.1 | 卡尺工具：沿指定方向找边缘点，支持多卡尺、极性、边缘对 | P0 | **测量** | ✅ |
| FR10.2 | 距离测量：点点、点线、点圆、线线 | P0 | **测量** | ✅ |
| FR10.3 | 角度测量（两线夹角、三点角度） | P0 | **测量** | ⚠️ 禁用的是 HALCON 版；按 `VisionIntegrationPolicy` 应改用 OpenCV 拟合线夹角实现，勿直接恢复 HALCON 注册（见 §3.21 / 路线图 G-P0-6） |
| FR10.4 | 圆/弧测量：直径、半径、圆心、圆度 | P1 | **测量** | ✅ |
| FR10.5 | 面积/周长测量 | P1 | **测量** | ✅ OpenCV Blob 面积端口已可用（沿用即可，不需 HALCON 版） |
| FR10.6 | 平整度、段差、间隙、体积、夹角（3D/深度图） | P2 | **3D 测量（VM 4.4）** | ❌ |
| FR10.7 | 测量结果支持上下限判定，输出 OK/NG | P0 | **测量** | ✅ |
| FR10.8 | 测量结果支持单位换算（像素↔毫米），依赖标定 | P0 | **测量** | ✅ |

### 3.11 识别（FR11.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR11.1 | 一维码识别：Code128/39/EAN/UPC 等主流制式 | P0 | **识别** | ✅ |
| FR11.2 | 二维码识别：QR、DataMatrix 等 | P0 | **识别** | ✅ |
| FR11.3 | 识别支持多角度、畸变、低对比度鲁棒性 | P1 | **识别** | ⚠️ 部分 |
| FR11.4 | 传统 OCR（印刷体，中英文） | P1 | **识别** | ✅ |
| FR11.5 | 深度学习 OCR（复杂背景、变形、低对比度） | P1 | **识别（DL OCR）** | ✅ |
| FR11.6 | 字符定位 + 字符识别分离的两阶段流程 | P2 | **识别** | ❌ |
| FR11.7 | 识别结果校验（正则/长度/校验位）与分级 | P2 | **识别** | ⚠️ 部分 |

### 3.12 缺陷检测（FR12.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR12.1 | 表面缺陷检测：划痕、斑点、脏污，克服纹理/颜色/噪点干扰 | P1 | **检测** | ✅ |
| FR12.2 | 轮廓/形状缺陷检测：毛刺、缺角、变形 | P1 | **检测** | ⚠️ 部分 |
| FR12.3 | **标准件比对**：与 OK 标准件模板比对，定位微小差异 | P1 | **检测** | ❌ |
| FR12.4 | 检测结果以热区/掩膜可视化，标注缺陷位置与面积 | P1 | **检测** | ⚠️ 部分 |
| FR12.5 | 缺陷分类（可自定义缺陷类别并输出类别标签） | P2 | **检测** | ⚠️ ClassifyNode 可用 |

### 3.13 深度学习（FR13.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR13.1 | 图像分类：支持 ONNX 模型导入与推理，输出 top-N 类别与置信度 | P1 | **DL 分类** | ✅ |
| FR13.2 | 内置训练工具：分类器训练（ANN_MLP）与推理闭环 | P2 | **DL 训练工具** | ✅（基础） |
| FR13.3 | **目标检测**：YOLO/SSD 类模型推理，输出类别+位置框 | P1 | **DL 目标检测** | ❌ |
| FR13.4 | **语义/实例分割**：像素级缺陷区域输出 | P1 | **DL 分割** | ❌ |
| FR13.5 | **异常检测**：仅用 OK 样本建模，输出异常热图 | P1 | **DL 异常检测** | ⚠️ 部分实现（2026-09-24）：**功能已交付但非 DL 模型**——`AnomalyDetectNode` 逐像素统计基线 + PCA 重构残差双后端，输出异常热图/数量/面积与异常框（见路线图 §0.1 ⑭）；仍缺 PatchCore/SPADE 类预训练特征先验 |
| FR13.6 | 字符定位与识别（DL） | P2 | **DL 字符识别** | ❌ |
| FR13.7 | 图像检索（以图搜图） | P3 | **DL 图像检索** | ❌ |
| FR13.8 | 小样本注册（Few-shot）：少量样本即可完成分类/定位/检测 | P2 | **DL 小样本** | ❌ |
| FR13.9 | 独立训练工具：图像打标、训练、评估、导出一体化 | P2 | **DL 训练工具（VM 4.4）** | ❌ |
| FR13.10 | 推理加速：支持 GPU/CPU 后端切换与批处理 | P2 | 性能优化 | ❌ |

### 3.14 标定与坐标变换（FR14.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR14.1 | 相机标定（棋盘格/标定板），输出内参与畸变 | P0 | 标定 | ✅ |
| FR14.2 | 手眼标定（像素→机器人坐标） | P0 | 标定 | ✅ |
| FR14.3 | N 点标定（像素→世界仿射） | P0 | 标定 | ✅ |
| FR14.4 | 标定数据集中管理（命名齐次矩阵）、随方案保存、JSON 持久化 | P1 | 标定管理 | ✅ |
| FR14.5 | 坐标系变换与位置修正节点 | P1 | 坐标变换 | ✅ |
| FR14.6 | 畸变校正（基于标定结果实时校正） | P1 | 标定 | ❌ |
| FR14.7 | 多相机联合标定（多视野坐标统一） | P2 | 标定 | ❌ |
| FR14.8 | 夹具/位姿管理（Fixture） | P2 | 定位治具 | ✅ |

### 3.15 逻辑与流程控制（FR15.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR15.1 | 条件分支 If-Else，按上游结果选择执行路径 | P0 | 逻辑工具 | ✅ |
| FR15.2 | 循环执行（固定次数/条件循环） | P1 | 逻辑工具 | ⚠️ 部分（2026-09-24）：**固定次数循环**已交付且 E5 已修复——循环体每轮重算激活集合（尊重条件分支）、迭代与节点边界双道暂停/停止检查、嵌套由内层自调度（`testLoopIterationOrder` / `testNestedLoopIterations`）；**条件循环（while 式）未做**：`LoopNode` 只注册了 `loopCount` 一个参数 |
| FR15.3 | 公式计算（表达式求值，支持数学函数与变量） | P1 | 逻辑工具 | ✅ |
| FR15.4 | 延时、计数、数据筛选、分类判定、排序 | P1 | 逻辑工具 | ✅ |
| FR15.5 | 执行模式：连续运行 / 软触发 / 硬触发 | P0 | 运行模式 | ✅ |
| FR15.6 | 单步执行、暂停/恢复、从指定算子执行/执行到指定算子（右键调试） | P1 | 调试 | ✅ |
| FR15.7 | 拓扑排序与环检测，阻止非法流程 | P0 | 流程校验 | ✅ |
| FR15.8 | 失败中断策略可配置（继续/中止/重试） | P1 | 异常处理 | ⚠️ 部分 |
| FR15.9 | 外部触发映射（字符串触发 / 接收事件触发 → 指定流程） | P1 | 触发管理 | ✅ |
| FR15.10 | 子流程（Sub-Flow）调用与复用 | P2 | 流程复用 | ⚠️ 部分（2026-09-24）：**设计期复用已交付**（`FlowSnippet` 子图复制/粘贴含内部连线、`.vfseg` 片段导入导出、单算子模板库）；**运行期调用已交付（v1）**——`SubFlowDef` 命名子图（存 extras，随方案与撤销快照走）+ 编辑菜单"定义为子流程…/删除子流程定义…" + 调用点算子 `SubFlowNode` + `FlowExecutor::executeSubFlow` 同线程内联调度（递归保护、未定义/定义损坏可见报错），`SubFlowTest` 6 用例钉住验收口径。v1 取舍：跨边界变量引用不支持（走调用点端口）、成员不得与循环体交叉、`subFlowName` 为文本参数非下拉；详见 `docs/子流程复用设计草案.md` §7 |
| FR15.11 | 断点与变量监视（调试器） | P2 | 调试 | ❌ |

### 3.16 通讯与 IO 控制（FR16.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR16.1 | TCP 客户端 / 服务器双模，支持文本与字节协议 | P0 | 通讯管理 | ✅ |
| FR16.2 | 串口通讯（RS232/485），参数可配 | P0 | 通讯管理 | ✅ |
| FR16.3 | Modbus TCP/RTU，主站与从站双模 | P0 | 通讯管理 | ✅ |
| FR16.4 | PLC 通讯：主流品牌（西门子 S7-1200/1500、三菱、欧姆龙、基恩士、松下） | P0 | 通讯管理 | ✅（经 Modbus TCP） |
| FR16.5 | 寄存器表配置：地址、数据类型（int16/int32/float）、字节序（ABCD/CDAB/BADC/DCBA）、读写模式 | P0 | 通讯配置 | ✅ |
| FR16.6 | 轮询周期与自动重连可配（默认 100ms / 3s） | P1 | 通讯稳定性 | ✅ |
| FR16.7 | 通讯心跳监测与掉线告警 | P1 | 通讯管理 | ✅ |
| FR16.8 | 通讯监视器（收发报文实时查看、模拟发送） | P1 | 通讯调试 | ✅ |
| FR16.9 | 接收/发送事件：文本协议、字节匹配、字节打包 | P1 | 协议处理 | ✅ |
| FR16.10 | **OPC UA 客户端** | P1 | 工业互联 | ❌ |
| FR16.11 | 原生 S7Comm（Snap7 等）/ EtherCAT / Profinet | P2 | 工业总线 | ❌ |
| FR16.12 | **数字 IO 板卡控制**（触发光源、气缸、剔除机构） | P1 | **IO 控制（VM 4.4）** | ❌ |
| FR16.13 | **光源控制器**（多通道亮度、频闪、触发同步） | P1 | **光源调节（VM 4.4）** | ❌ |
| FR16.14 | 机器人引导通讯（视觉坐标下发，支持常见机器人协议） | P2 | 机器人引导 | ⚠️ 部分 |

### 3.17 数据输出、存储与报表（FR17.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR17.1 | 图像/Region/数值/字符串结果显示与缩放查看 | P0 | 结果展示 | ✅ |
| FR17.2 | 结果图像与数据写文件（本地存档） | P1 | 数据输出 | ✅ |
| FR17.3 | 检测记录入库（SQLite），含时间、结果、关键数值 | P1 | 数据存储 | ✅ |
| FR17.4 | 报警记录与历史查询（Info/Warning/Error 分级） | P1 | 报警管理 | ✅ |
| FR17.5 | 操作日志审计（谁在何时改了什么） | P1 | 审计 | ✅ |
| FR17.6 | 统计报表：良率、NG 分布、趋势图，支持导出 CSV/Excel | P2 | 报表 | ⚠️ 部分（2026-09-23 补：**良率目标线 + 报警联动**已交付——目标线与达标判定、按"最近 N 轮"的滚动窗口报警、报警写 `FR17.4` 的报警历史；仍缺：趋势粒度只到"天"、定时导出） |
| FR17.7 | 数据过期清理策略（保留天数可配，默认 30 天） | P2 | 数据维护 | ✅ |
| FR17.8 | 结果图片自动命名与分类归档（OK/NG 分目录） | P2 | 数据管理 | ⚠️ 部分 |
| FR17.9 | 支持 MES/上位系统数据上传（HTTP/数据库直写） | P2 | 信息化对接 | ❌ |

### 3.18 平台服务（FR18.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR18.1 | 用户权限：Admin / Engineer / Operator 三级角色，按角色限制编辑/运行/用户管理/通讯配置 | P1 | 用户管理 | ✅（角色矩阵本身早已实现；**此前裸 ✅ 漏了一条越权入口，2026-09-25 由阶段 A 的 A1-② 关闭**）。原缺口：取消/关闭登录框后 `MainWindow` 仍构造出 guest 会话并**默认赋 Operator 角色**（能跑流程、能往库里写检测结果，操作日志里全是 guest，事后查不到是谁）。现改为不建会话、不进主界面——`MainWindow` 构造期只在 `exec()==Accepted` 时置 `m_loginAccepted`（`src/MainWindow.cpp:585`～`:593`，标志在 `include/MainWindow.h:293`，默认 `false`），`src/main.cpp:349`～`:351` 见 `loginAccepted()==false` 直接 `return 0`；同一改动删掉了 `authenticateUser("admin","admin")` 兜底。**未实测**：取消后进程确实零窗口退出只经编译与代码路径核对，未做人工点击验证 |
| FR18.2 | 登录认证与密码安全存储（加盐迭代哈希） | P1 | 用户管理 | ✅（哈希存储一直成立；**但"认证"此前是半成品，裸 ✅ 属过度声明——2026-09-25 由阶段 A 的 A1-① 关闭**）。原缺口：`AppDatabase` 建表时自动插入 `admin`/`admin`（`src/AppDatabase.cpp:209`～`:219`，老库升级兼容路径），出厂口令**永远可用**，历史实现只在登录后弹一次告警即放行 ⇒ 绝大多数现场的实际运行态是"无鉴权"。现改法（判定与落库不碰控件，可脱机单测）：`AppDatabase::isFactoryAdminPasswordInUse()`（`:423`，口令字面量全仓仅此一处）+ `changeUserPassword()`（`:403`，**影响 0 行即报失败**，防"用户不存在却解锁"）→ `FactoryPasswordGuard::{factoryPasswordInUse,refreshWritesLock,retireFactoryPassword}`（`src/FactoryPasswordGuard.cpp:19`/`:24`/`:31`）→ `SessionManager::setWritesBlocked()`（`src/SessionManager.cpp:41`，位在建会话处由 `refreshWritesLock()` 按库实际状态刷新），闸折叠进 `canManageUsers`/`canEditScheme`/`canConfigCommunications`（`src/SessionManager.cpp:50`～`:68`；接口位 `include/SessionManager.h:38`～`:39`）。**闸落在持久化层而非只做菜单灰化**（灰化挡不住快捷键与后续新增调用点）：`writeGateRefusal()`（`src/ProjectManager.cpp:45`）在 `saveProjectInteractive`（`:66`）/`saveProject`（`:127`）/`exportEncryptedProject`（`:250`）三处**先挡再改状态**。UI 侧 `MainWindow::enforceFactoryPasswordPolicy()`（`src/MainWindow.cpp:3898`）登录后调用（`:602`），当场要求改密、失败不退出循环、放弃则挂常驻状态栏提示，并保留**始终可点**的「系统 → 修改管理员口令」（`:3809`～`:3816`，改密前必须填对当前出厂口令）。`canRunFlow()` **有意不加闸**（现场不能因为没改口令就停线），有专门用例钉住这条边界。<br>验收：`FactoryPasswordGuardTest` 8 条用例全绿（含"改密成功后写闸解除"与"解锁后可保存"的反向对照）；**改坏自证**：临时去掉 `ProjectManager` 写闸 ⇒ `saveProject_refusedWhileWritesBlocked` 变红，恢复后转绿。<br>**未实测**：模态改密对话框未做人工点击验证（策略逻辑脱机覆盖，UI 层仅编译与菜单可达性核对） |
| FR18.3 | 运行日志与日志分级、日志检索 | P1 | 日志 | ✅ |
| FR18.4 | 中英文双语界面（中英文名 + 别名，双语搜索） | P1 | 国际化 | ⚠️ 部分：注册表已带中英双名（`displayName`/`englishName`+别名），界面语言切换见 FR18.5（已翻译 230 条）。**未做**：工具箱/Ctrl+F 搜索均不匹配 `englishName` 与别名（前者只匹配中文名+类型 id，后者只匹配节点名+模块号），英文模式下算子名仍显中文 |
| FR18.5 | 完整 Qt 国际化方案（QTranslator + .ts/.qm，覆盖全部 UI 与算子名） | P2 | 国际化 | ⚠️ 部分实现（2026-09-24，v1）：`I18n`（QSettings 持久化语言、重启生效、`.qm` 缺失时降级中文）+ 语言开关（简体中文/English）+ lupdate/lrelease 管线随构建部署 `translations/visionflow_en.qm`；主干已翻译 230 条（`I18nTest` 端到端验证）。**未做**：参数面板提示、运行日志等硬编码中文字面量批量补 `tr()`（G-P1-5 剩余项）；算子名/分组名取自注册表硬编码中文，不经翻译（见 FR18.4） |
| FR18.6 | 工业深色主题与品牌定制 | P2 | 界面风格 | ✅ |
| FR18.7 | 内置帮助手册（F1）与算子级帮助文档 | P1 | 帮助系统 | ✅ |
| FR18.8 | 环境自检（HALCON/相机/依赖库可用性诊断） | P1 | 系统诊断 | ✅ |
| FR18.9 | 算子自检与代码导出 | P2 | 开发工具 | ✅ |
| FR18.10 | 方案自动备份与崩溃恢复 | P2 | 数据安全 | ✅ 已交付（2026-09-23：自动保存 + 崩溃恢复 + 关窗未保存确认，见 `RecoveryStore`） |

> FR18.10 实现口径：自动保存默认每 **60 秒**一次（`recovery/autoSaveIntervalSec`，置 0 关闭），
> **内容与已落盘内容一致时不写盘**；恢复文件 `<应用目录>/data/recovery/recovery.vfp`
> （安装目录不可写时自动回落用户数据目录）。恢复文件本身是**合法方案文件**，也可用「文件→打开方案」
> 直接打开。启动时若发现恢复文件会询问"恢复 / 丢弃"；关窗时有未保存改动会提示
> （`recovery/promptOnClose`，关闭后保留恢复文件交由下次启动询问）。
> 「打开方案 / 新建方案」前同样先确认未保存改动（同一提示总开关）——原先只有退出这一条路有保护，
> 新建/打开会静默丢弃当前修改。

### 3.19 扩展与二次开发（FR19.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR19.1 | 脚本算子：支持 Python / Lua 扩展逻辑 | P1 | **算子设计模式** | ✅ |
| FR19.2 | 脚本安全策略：语言白名单、解释器隔离、环境变量清理、执行确认、超时、审计日志 | P0 | 安全 | ✅（**"环境变量清理"此前对 Lua 是空档，裸 ✅ 属过度声明——2026-09-25 由阶段 A 的 A1-③ 关闭**）。原缺口：`ScriptSecurityPolicy::buildEnvironment()`（`src/ScriptSecurityPolicy.cpp:264`）的黑名单只有 `PYTHON*` / `LD_*` / `DYLD_*` / `BASH_ENV` / `ENV`，**没有任何 `LUA_*`** ⇒ 攻击者只要能在宿主环境留下 `LUA_INIT`（或版本专有的 `LUA_INIT_5_4`，优先级更高）就等于在脚本执行前插入一段任意 Lua 代码，`LUA_PATH*`/`LUA_CPATH*` 还能把 `require` 重定向到恶意模块。现补 6 条（`:289`～`:294`），黑名单共 19 条。`PATH` 仍保留（否则找不到解释器本体）。**刻意没给 Lua 加 `-E` 类命令行标志**：`LUA_*` 只有官方 `lua` 可执行文件认，本机无 `lua.exe` 可验证目标解释器是否接受该标志，加错会让 Lua 直接起不来，而现有用例钉住"Lua 隔离标志为空"（`tests/script_security_test.cpp:73`，`interpreterFlags_isolation`）；收敛点是 `buildEnvironment()` 本身——普通分支与沙箱分支（`runWithRestrictedToken()` 内部同样调它，`src/ScriptSecurityPolicy.cpp:847`）共用一处，无绕过路径。<br>验收：`ScriptSecurityPolicyTest::buildEnvironment_stripsDangerousVars`（`tests/script_security_test.cpp:76`）绿——先把 10 条注入变量**真实写入父进程环境**再断言子环境不含（避免"父进程本来也没有"的空断言），并带 `setStripEnvironment(false)` 反向对照证明透传路径未坏。<br>**改坏自证**：临时从黑名单删掉 `LUA_*` ⇒ 该用例以 `LUA_INIT leaked into child env` 变红，恢复后转绿 |
| FR19.3 | 自定义算子插件机制（动态库注册新算子，无需改主程序） | P1 | **算子设计模式** | ❌ |
| FR19.4 | 对外 SDK：可被第三方程序控制（加载方案、触发、取结果） | P2 | **SDK 二次开发模式** | ❌ |
| FR19.5 | 算子/方案市场与行业方案包 | P3 | 生态 | ❌ |

### 3.20 3D 视觉（FR20.x）

| 编号 | 需求描述 | 优先级 | VM 对标 | 状态 |
|------|---------|--------|---------|------|
| FR20.1 | 3D 相机/深度图接入（点云/深度图数据类型） | P2 | **3D 升级（VM 4.4）** | ❌ |
| FR20.2 | 点云预处理（滤波、降采样、平面分割） | P2 | **3D 预处理** | ❌ |
| FR20.3 | 3D 轮廓匹配 | P2 | **3D 轮廓匹配** | ❌ |
| FR20.4 | 3D 特征提取与测量（平整度、段差、间隙、体积） | P2 | **3D 测量** | ❌ |

> **3D 范围说明**：海康官方将 VM3D 作为独立产品平台介绍，3D 能力不应在未核实版本的情况下全部计入 VM 4.4 基础对标缺口；是否纳入 VM 4.4 对标、还是对标 VM3D，需单独论证（见路线图 G-P2-1）。

---

### 3.21 执行引擎正确性（2026-09-14 提出 → 2026-09-25 二次复核：一项干净、四项部分修复、一项代码已修用例未钉住 → 同日阶段 A 的 A4：E1/E2/E3 转为"已修复并钉住"，E6 断言已钉住而默认节拍策略仍开放）

> 依据 2026-09-14 代码审查（`FlowExecutor.cpp` / `MvsImageSourceNode.cpp` / `DelayNode.cpp` / `LoopNode.cpp`），当时结论为"须先修复才可对标工业交付"。
> **2026-09-24 复核曾写"六项全部已修复"——该结论过度，2026-09-25 按代码逐条重读后予以降级。** 降级依据不是猜测，而是每条都读到了具体的剩余缺口（见下表「残留」小段，均带文件与行号）。
> **本次分级口径**：`已修复并钉住` = 机制已改 + 有真跑用例断言该机制；`部分修复` = 主路径已改但存在明确未覆盖缺口；`用例未钉住` = 代码已改但无断言该行为的用例。
>
> **A4 之后的一节总账（2026-09-25）**：六项现分级为 **E1/E2/E3/E4/E5 = 已修复并钉住**，**E6 = 断言已钉住、默认节拍策略未拍板（开放项 O-1）**。A4 新增 6 条 `IntegrationTest` 用例：`testParamRefRestoredBeforeNodeSignals`、`testNonStdExceptionInBroadcastIsContained`、`testParamRefRestoredWhenNodeThrowsBeforeSignals`（E1）、`testSkippedAndFailedNodesLoseReusableMark`（E2）、`testOwnerExecutorEstablishedAtGraphBuild`（E3）、`testRoundRateNotClampedByFixedInterval`（E6），每条都做过"故意改坏 → 变红"的自证，命中位置与消息见各表行末段。**全量门禁（A4 改动全部还原后的整树重编 + 全跑）实测**：`100% tests passed, 0 tests failed out of 29`，`Total Test time (real) = 102.45 sec`；**时间口径**：下表各行写的"A4 收口（2026-09-25 同日）"指代码与用例落地当天（25 日深夜），逐条改坏自证与"复原 → 重编 → 全跑"收在 **2026-09-26 上午**，上面这次门禁的日志时间戳为 2026-09-26 09:17。复核者可自行复现：在 `build/` 下执行 `ctest -C Release`（**必须带 `-C Release`**，且运行测试程序需把 `dist/VisionFlowPlatform` 前置到 `PATH`，否则 `opencv_world4130.dll` 加载失败）。**本节仍不整体判闭合**，两个理由：① E6 的默认节拍策略属需求方拍板项，不在工程侧可关闭；② 上表 E1/E2/E3 三行各自末尾的"**本轮未收口的边界**"是明写的开放缺口（handler 二次抛出、`m_validOutputs` 至今无读者、建图前窗口内新增节点短暂无主），逐条读原文再判断，不要只看行首的 ✅。

| 编号 | 原问题（2026-09-14） | 现状（2026-09-25 二次复核，逐条读到代码与用例） |
|------|---------------------|--------------------------------------|
| E1 | 参数引用 `{模块号.参数名}` 解析结果被 `setParam` 写回参数，第二轮沿用旧值、存盘可能把引用存成常量 | ✅ **已修复并钉住（2026-09-25 阶段 A 的 A4 收口，改法与逐条改坏自证见本行末段）**。<br>【以下整段为 2026-09-25 二次复核记录，**原文保留、现指"改前状态"**】⚠️ 部分修复。已修：执行前把表达式备份进 `paramRefBackups` 并**仅本轮**临时替换为解析值，执行后统一还原表达式（`FlowExecutor.cpp:888`～`:906` 备份替换、`:1078` 还原；源码注释即标 E1）⇒ 跨轮重新解析成立。`testParamRefResolvesEachRound` 真跑连续模式两轮，第一轮 8、第二轮 10（源值改为 9 后重新解析），并断言参数里仍含 `{`。<br>**残留（2 条）**：① 还原循环在 `try/catch` **之后**（`:1075` 闭括号 → `:1078` 还原），而 `emit nodeExecuted`（`:982`）、`nodeOutputsUpdated`（`:995`）、`imageAvailable`（`:1023`）、`imageReady`（`:1066`）全部发生在"参数仍是解析值"的窗口内 ⇒ 该窗口内触发的保存/快照会把引用落成常量，原问题只在"跨轮"维度关闭，"存盘成常量"维度仍开放；② 该 `try` 只捕 `const std::exception &`（`:1070`）**无 `catch(...)`**，非标准异常沿栈展开时还原循环被整段跳过，参数永久停在常量上。<br>**A4 收口（2026-09-25 同日，阶段 A）**：两条残留各自关闭。<br>① **还原必须早于任何对外广播**：新增 RAII 还原器 `ParamRefRestorer`（类体 `src/FlowExecutor.cpp:912`～`:938`，所在匿名 `namespace` 块 `:906`～`:940`；析构即 `restore()`，`restore()` 幂等——还原后清空备份表），并在 `executeNode` 里把**显式还原**放在第一条对外广播之前（`:1062`，紧接其后的 `emit nodeExecuted` 起那五条广播全部落在"参数已还原"的一侧）。② **补 `catch (...)`**（`:1158`～`:1167`），两个 handler 均先 `paramRefs.restore()`（`:1154`、`:1163`）再把 `success=false` 上报。**顺带修掉一处自相矛盾**：旧写法在 handler 里写 `m_lastNodeSuccess=false`，却被函数尾的 `m_lastNodeSuccess = success`（异常前可能已是 `true`）覆盖 ⇒ "报了 `executionError` 却不拦停"；现改为 handler 内置 `success=false`、尾部单一赋值（`:1171`）。<br>**新用例（3 条，逐条改坏自证，互不冗余）**：`testParamRefRestoredBeforeNodeSignals`（`tests/integration_test.cpp:1180`）用三条直连槽逐一取参数，断言窗口内仍含 `{` 且解析确实发生（`value==8.0`）——把 `:1062` 的显式还原注释掉后该用例判红，消息点名 `nodeExecuted=7 + 1; nodeExecutionTime=7 + 1; nodeOutputsUpdated=7 + 1`，另两条 E1 用例保持绿；`testNonStdExceptionInBroadcastIsContained`（`:1246`）在 `nodeOutputsUpdated` 的直连槽里一次性抛**非标准**异常，断言进程存活、`executionError` 有发、本轮按失败收口、参数引用仍在，且随后一轮仍能算出 8——把 `catch (...)` 整段删除后该用例判红；`testParamRefRestoredWhenNodeThrowsBeforeSignals`（`:1316`）用探针 `ThrowBeforeRestoreProbeNode`（`:1156`，在 `collectNodeOutputVars` 内、即还原点**之前**抛）钉住异常路径的还原——把析构体清空**且**去掉 `catch (...)` 里那行还原后，该用例的"参数引用没被还原"断言（当前 `:1356`）判红。<br>**证据更正（归因不得强于证据）**：此前推测"非标准异常逃出即 `std::terminate`"。删掉 `catch (...)` 的实测结果是两条用例均以 QtTest 自带的 catch-all 判红（失败位置 `qtestcase.cpp:2000`，消息 `Caught unhandled exception`），**并未**中止进程；"逃出即中止"只适用于工作线程 `run()` 这条没有 catch 的路径（静态核实：全文件仅 `executeNode` 内一处 `try`），该路径本轮**未真机取证**。<br>**本轮未收口的边界（如实记录）**：a) handler 自身再抛出（例如 `executionError` 的直连槽二次抛异常）仍会逃出 `executeNode`；b) 抛出本轮该节点会先收到 `nodeExecuted(...,true)`、再收到补发的 `nodeExecuted(...,false)`（两条信号不代表跑了两次）——这是 handler 的既有形态，本轮只把"最后一条必须为 false"钉进用例，未改信号协议。 |
| E2 | 未激活分支只清节点输出、未清 `m_nodeData`，汇合节点可能用上一件产品数据 | ✅ **已修复并钉住（2026-09-25 阶段 A 的 A4 收口，改法与逐条改坏自证见本行末段）**。<br>【以下整段为 2026-09-25 二次复核记录，**原文保留、现指"改前状态"**】⚠️ 部分修复（残留当前不可达，属潜伏）。已修：跳过分支同时清空输出端口与 `m_nodeData` / `m_nodeOutputVars`，并发 `nodeSkipped`（`FlowExecutor.cpp:1246`～`:1256`，源码注释即标 `E2/E5/P2`）。`testConditionalBranchSkipClearsStaleOutput` + `testContinuousSecondRoundClearsStaleData`。<br>**残留**：同一段清理**未清 `m_validOutputs`**。当前无害仅因"增量复用"分支已回退（`FlowExecutor.cpp:912` 处【待办·增量执行复用】注释即该回退标记）；一旦恢复复用优化，`m_validOutputs` 的陈旧"已有效"位会让汇合节点重新读回上一件产品数据，E2 原症状原样复发。因此不能判"已修复"。<br>**A4 收口（2026-09-25 同日，阶段 A）**：采用的不变量——**凡整体清空某节点的输出端口 / 其 `m_nodeData` 条目、且本轮不再重跑该节点 ⇒ 同步撤销 `m_validOutputs`；整表清空 ⇒ 整表撤销**。据此补齐五处撤销位点（改后行号）：`src/FlowExecutor.cpp:585`（`run()` 主遍历"分支未激活"跳过）、`:1041`（`executeNode` 失败分支）、`:1343`（`executeLoop` 循环体内跳过）、`:1520`（`executeSubFlow` 入口节点本轮无喂入）、`:1636`（`resetState()` 整表）；成功路径里逐端口的 `m_nodeData[node].remove(i)`（`:1019`）**有意不撤销**——同一轮紧接着就会重新置位。另因该位此前**无任何读者**（增量复用分支仍处回退状态——该回退标记注释【待办·增量执行复用】现在在 `src/FlowExecutor.cpp:981`，「原文保留」段里写的 `:912` 是改前快照），"潜伏"无法从外部断言，故新增只读视图 `FlowExecutor::isOutputMarkedValid(NodeBase *) const`（`:145`，取 `m_graphCacheMutex`）作观测点。<br>**新用例 `testSkippedAndFailedNodesLoseReusableMark`（`tests/integration_test.cpp:1360`，四阶段）**：① 正向锚点（成功产出必须被标记，否则后面全是"本来就空"的假绿）；② 失败清空；③ 连续模式跑两轮翻分支 ⇒ 上轮成功过的 `trueB` 本轮被跳过；④ **同一个执行器**重启（观察点选 `executionStarted`：`resetState()` 在 `startExecution()` 里、线程起来前就跑完）。④ 一开始写成"换个新执行器"——那是**恒真断言**（`m_validOutputs` 是执行器私有表，新表天然为空），已改写。<br>**逐处改坏自证（每次只坏一处，三处分别命中三个阶段）**：注释 `:1041` ⇒ 用例的"失败分支未撤销"断言（当前 `tests/integration_test.cpp:1393`）判红；注释 `:585` ⇒ "被跳过的节点仍带标记"断言（当前 `:1421`）判红；注释 `:1636` ⇒ "重启后仍保留上一轮标记"断言（当前 `:1444`）判红。三处当时日志报告的行号分别是 1391/1419/1441，本表数字**按 +3 重定位**（该用例上方在此之后新增了 3 行注释，源文件未再动过；识别以 FAIL 消息文本为准）。<br>**本轮未收口的边界（如实记录）**：a) `:1343`（循环体跳过）与 `:1520`（子流程无喂入）两处与已证位点**同形态**，但本轮**没有**独立用例覆盖 ⇒ **未做改坏自证**，正确性靠同一不变量的代码走查；b) 该位今天仍无读者，本项关闭的是"标记与实际数据不一致"这一**潜伏**缺陷，不是已发生的错数据——E2 原症状要等复用优化恢复才可能显形，届时该位已由本用例钉住。 |
| E3 | 多流程共享全局 `s_currentInstance`，切到流程 B 后流程 A 的相机按 B 的状态等待/退出 | ✅ **已修复并钉住（2026-09-25 阶段 A 的 A4 收口，改法与逐处改坏自证见本行末段）**。<br>【以下整段为 2026-09-25 二次复核记录，**原文保留、现指"改前状态"**】⚠️ 部分修复。已修：**执行路径**上节点优先取 `ownerExecutor()`，全局 `FlowExecutor::current()` 仅作无主兜底（`MvsImageSourceNode.cpp:426`、`:819`、`:1061` 三处同口径），硬触发等待/取图不再被别家流程的状态左右。`testTwoExecutorsIsolation` 钉双流程隔离。<br>**残留**：`setOwnerExecutor()` **全仓只有一处调用**——`FlowExecutor.cpp:881`，位于单节点执行函数内。⇒ 节点归属只在"该节点真正被跑过一轮"之后才建立；在此之前 `ownerExecutor()` 恒为空，`:819`（应用参数时判断是否连续/运行中，决定是否跳过像素格式写入）与 `:1061`（`refreshPixelFormatEnabled` 决定下拉框启用/置灰）这两条 **UI 触发路径必然回落 `current()`**，即"最后启动的那条流程"。双流程下 A 的相机参数面板会按 B 的运行状态被锁/被放行。要真正关闭需在执行器建图/入队时就建立归属，而非等首次执行。<br>**A4 收口（2026-09-25 同日，阶段 A）**：按上述要求在**两个建图时点**建立归属，不再等首次执行——① `FlowExecutor::setFlowScene()`（`src/FlowExecutor.cpp:257`～`:266`，归属循环在 `:259`～`:263`）：挂上场景即遍历 `scene->nodes()` 逐个 `setOwnerExecutor(this)`，并把"最近激活流程即 `current()`"的 `s_currentInstance = this`（`:265`）收进同一个 `if (scene)` 守卫（解绑 `scene == nullptr` 时**不清**归属：节点可能已转由别的执行器接管，清成空反而退回 `current()` 兜底）；② `rebuildIncomingIndex()`（`:777`～`:781`）：对**本轮快照里的全部活节点**建归属，该函数是 `run()` 轮首、`executeUpTo`、`executeFrom`、`invalidateDownstreamOf` 四条入口共同的建图点 ⇒ 中途新增的节点同样在下一轮建图时补上。`executeNode` 里原有那处（`:949`）保留，二者同口径。<br>**新用例 `testOwnerExecutorEstablishedAtGraphBuild`（`tests/integration_test.cpp:1450`，四阶段）**：① 接入场景前归属必为 `nullptr`（前置，否则后面分辨不出是谁建立的）；② `setFlowScene` 之后**一次都不跑**即断言归属 == 执行器；③ 接入**之后**新增、且整轮都被条件分支跳过（从未进过 `executeNode`）的节点仍有归属；④ 双流程并发：B 已抢走 `current()`（`QTRY_VERIFY` 断言前提成立）时，A 的三个节点归属仍为 A、B 的节点归属为 B。<br>**逐点改坏自证（两处分别命中不同阶段）**：注释 `setFlowScene` 的归属循环 ⇒ 用例在阶段② 的 `QCOMPARE(cond->ownerExecutor(), &execA)`（当前 `tests/integration_test.cpp:1471`）判红（`Actual (cond->ownerExecutor()): <null>`）；注释 `rebuildIncomingIndex` 的归属循环 ⇒ 在阶段③ 的 `QCOMPARE(falseB->ownerExecutor(), &execA)`（当前 `:1492`）判红——后者正是"从未执行过的节点"这一原始症状。两条日志当时报告的行号是 1468 与 1489，本表数字**按 +3 重定位**（该用例上方在此之后新增了 3 行注释；识别以 FAIL 消息文本为准）。<br>**本轮未收口的边界（如实记录）**：a) "挂上场景之后、下一次建图之前"这个窗口内新增的节点仍短暂无主（此窗口内打开相机面板依旧回落 `current()`）；要彻底关闭需在 `FlowScene::nodeAdded` 里直接写归属，而那会把"GUI 线程写 `ownerExecutor`"这种跨线程裸指针写引入现有只在执行线程写的模式，本轮按最小改动原则**不做**；b) `MvsImageSourceNode.cpp:426/:819/:1061` 本轮**未改**，其"有主取主、无主取 `current()`"的回落逻辑保持原样——本项关闭的是"归属建立得太晚"，不是重写那两条 UI 路径。 |
| E4 | 延时 `QThread::msleep` 不可取消；执行器析构仅 `wait(5000)`，超时仍销毁可能访问已释放成员 | ✅ **两半均已修并钉住（后半 2026-09-25 由阶段 A 的 A1-④ 关闭）**。前半：延时改为**可取消等待**——有所属执行器时走 `FlowExecutor::interruptibleSleep(delayMs)`（停止/暂停立即唤醒，`DelayNode.cpp:36`～`:43`，源码注释标 E4），仅在无主执行器时才退回 `msleep`；停止与暂停在节点边界与迭代边界都生效。`testDelayStopCancellable` / `testScriptNodeStopCancellable`。<br>后半（原文记录的残留，现已关闭，保留以便追溯）："`~FlowExecutor()` 仍是 `if (!wait(5000)) { VFP_DEBUG ... }`——**超时只记一条日志，随后照常 `disconnectFromScene()` 并继续析构全部成员** ⇒ use-after-free 风险原样存在；`testDestroyWhileRunningIsSafe` 走的是快路径，恰好绕开超时分支，**不能据它判本项通过**。"<br>**现改法**：等待逻辑收成唯一实现 `FlowExecutor::joinForDestroy(int timeoutMs)`（`src/FlowExecutor.cpp:57`，语义 = 停止 + 只在真需要时等）；析构（`src/FlowExecutor.cpp:66`）走 `joinForDestroy(kDtorJoinTimeoutMs)`（常量在 `:55`，5000 ms），**超时即 `qCritical`（带 `workerPhaseName()` 与状态）+ `qFatal`（`:78`～`:84`）而绝不再继续拆场景**——继续析构等于在线程还活着时释放它持有的对象，静默 UAF 比中止更坏。所有权侧的泄漏保护 `MainWindow::retireExecutor`（`src/MainWindow.cpp:662`）本就正确（超时不强杀、转 `finished` 自删），本轮只把它的等待判据换成同一函数，未重复实现。<br>**新增用例**：`IntegrationTest::testJoinForDestroyTimeoutRefusesDestroy`（定义在 `tests/integration_test.cpp:631`，声明 `:104`；探针子类 `StuckExecutor` 在 `:63`）用覆写 `run()` 的自旋线程直接钉超时分支——`joinForDestroy(300)` 必须返回 `false` 且线程仍在跑，放行后才允许销毁；旧用例走快路径的盲区从此被覆盖。 |
| E5 | `LoopNode::run()` 初值 `moduleStatus=false` 被基类判失败；`runLoopBody` 强制激活全部循环体、绕过条件分支、不检查暂停/停止 | ✅ 已修复：`LoopNode::run()` 透传即置 `moduleStatus=true`（源码注释标 E5）；原 `runLoopBody` 已不存在，改为 `FlowExecutor::executeLoop` 统一调度——**每轮重算激活集合以尊重条件分支**（`FlowExecutor.cpp:1202`）、**迭代与节点边界双道暂停/停止检查**（`:1223`、`:1232`）、嵌套循环由内层自调度。`testLoopIterationOrder` / `testNestedLoopIterations` / `testExecutorReuseAcrossScenesWithLoop` |
| E6 | 连续与硬触发每轮固定 `msleep(50)`，算法不耗时也被限到约 20 轮/秒 | 🔶 **原诉求已达成且已钉住（2026-09-25 阶段 A 的 A4）；但默认节拍策略仍未拍板 ⇒ 本行不判"已修复"，悬挂在开放项 O-1**。<br>【以下以「」括起的是 2026-09-25 二次复核记录，**原文保留、现指"改前状态"**】「⚠️ **代码已改，用例未钉住，且改动本身引入新开放项**。已改：改为 `m_loopIntervalMs`（`include/FlowExecutor.h:235`，**默认 0 = 不额外限速**），仅 >0 时才 sleep（`FlowExecutor.cpp:670`～`:671`）。<br>**残留 ①（用例侧未钉住）**：全仓只有 `testNodeChurnDuringContinuousRun`（`tests/integration_test.cpp:420`）显式 `setLoopIntervalMs(0)`（`:433`）以消除节拍对压测的干扰，没有专门断言"两轮间隔不再被固定 50 ms 拉住"的用例；`testExecutionTime` 测的是单节点耗时、`testRuntimeStatsCounters` 测的是统计计数，都不能替代该断言。<br>**残留 ②（新风险，原问题反过来）**：默认值取 0 等于把"限速"这个责任整体交给用户。上一版症状是"慢算法被固定 50 ms 拖住"，本版症状反过来变成"快算法满负荷空转"——单轮算法只需几毫秒的连续流程会尽 CPU 之能地轮询采集/触发，功耗、CPU 占用与相机/通讯的调用频次都失去上界。正确性上 E6 的原诉求（不被固定节拍限死）已达成，但**默认节拍策略仍未定**，属需需求方拍板的开放项，不能记作已修复。」<br>**A4 收口（2026-09-25 同日，阶段 A）——只关残留 ①，按计划约束"拍板前不改默认值"**：新增 `testRoundRateNotClampedByFixedInterval`（`tests/integration_test.cpp:1515`，声明 `:121`）——连续模式跑满 10 轮，用 `nodeExecuted` 的直连槽逐轮取时间戳，断言**平均** `perRoundMs < 25.0`（口径写在用例注释里：固定 50ms 节拍下 9 个间隔必然 ≥450ms，阈值取一半既判得住"没被 50ms 拉住"，又给慢机留一倍余量）。**该用例故意不调 `setLoopIntervalMs`**，测的就是出厂默认；否则钉住的是自定义节拍而非默认值，正是原"没有用例覆盖"的空洞形态。当前行号（改后重测）：默认值在 `include/FlowExecutor.h:238`，节拍判断在 `src/FlowExecutor.cpp:691`～`:692`。<br>**改坏自证（两个实测数值即从这里读出）**：① 把 `include/FlowExecutor.h:238` 的默认值改回 `50` ⇒ 本用例判红，消息 `两轮间隔被固定节拍拉住：平均 62.22ms/轮（阈值 25ms，旧缺陷为固定 50ms）`，`Totals: 2 passed, 1 failed, ... 567ms`；② 恢复 `0` 后临时把阈值改成恒假以读出绿值 ⇒ `平均 0.11ms/轮`。这两个数只说明"两轮间隔不再被固定节拍拉住"，**不构成生产可用节拍或吞吐指标**。<br>**仍开放（本轮明确不做）**：上面「残留 ②」一字未改——默认 0 把限速责任整体交给用户，快算法连续流程满负荷空转，功耗/CPU 占用/相机与通讯调用频次无上界。**是否限速、限多少属开放项 O-1，需需求方拍板**；在此之前本行记为"断言已钉住 + 默认策略未定"。 |

> **行号口径（2026-09-25）**：A1-④ 在 `src/FlowExecutor.cpp` 顶部净增 22 行（`git diff -U0` 的两个 hunk：`@@ -52,0 +53,13 @@`、`@@ -59,4 +72,13 @@`），本表所有 `FlowExecutor.cpp:*` 因此整体下移。**上表数字不是按 +22 换算得来的，而是逐条重测**（`grep -n "paramRefBackups" src/FlowExecutor.cpp` 等），因为旧表里有至少一处引用本身就与当时行数差 1，换算会把误差固化。
>
> **A4 之后的补充口径（2026-09-25 同日）**：A4 又改了同一批文件（`src/FlowExecutor.cpp`、`include/FlowExecutor.h`、`tests/integration_test.cpp`），**且 A1～A4 均未入库**，所以无法再像上面那样用 `git diff` 把 A4 的净增行数单独量出来（工作树里没有 A4 改前的基线可比）。【2026-09-26 就地更新，原文保留】阶段 A 已入库（提交 `0be8675`／`bee922f`／`3726541`，基线 `29dd062`）⇒ 比对前提已恢复，本句描述的是写下当时、不是现在的状态。结论性规则：**本表凡标"A4 收口"的段落里的行号 = 当前实测**（逐条 `grep -n` 重读源码得到）；**「原文保留」段内的行号一律按"该次复核当时的快照"读，不要拿去跳**。复核锚点（本轮实测，便于读者判断自己看到的是哪个版本）：`src/FlowExecutor.cpp` 1912 行、`include/FlowExecutor.h` 304 行、`tests/integration_test.cpp` 3419 行；`IntegrationTest` 的测试函数个数按 `grep -cE "^void IntegrationTest::test" tests/integration_test.cpp` 数 = **50**（口径即这条命令，A4 前为 44，新增 6 条全部记在上表 E1/E2/E3/E6 四行的"A4 收口"段）。
>
> **安全四条（P0-2）不在本节记录**：本节只管执行引擎正确性，A1 四项里只有 ④ 属于引擎（已写进上表 E4 行）。①②③ 的落点、验收与改坏自证记在 §3.18（`FR18.1`、`FR18.2`）、§3.19（`FR19.2`）与 §4.4（`NFR4.1`/`NFR4.5`/`NFR4.6`）。此前这几行的**裸 ✅ 属反向过度声明**（声明的能力比代码实际做的多），本轮按"原文保留 + 就地更正"处理。

**2026-09-25 新发现并已修复：P0-1 —— 过半注册节点被基类契约恒判失败（不在上表 E1–E6 之内，同属执行引擎正确性）**

> 原问题：`HalconNode::init()` 把 `moduleStatus` 初值化为 `false`，而 `HalconNode::process()` 在 `run()` 之后检查该位、为 `false` 即清空全部输出并 `return false`。本族多数 `run()` **只在 `catch` 分支写 `moduleStatus=false`、成功路径从不写位** ⇒ 这些算子被拖进流程即恒判失败；叠加 `m_stopOnFailure` 默认 `true`（`include/FlowExecutor.h:233`），一条流程会被这类"假失败"直接拦停。`NodeBase` 层完全不认识 `moduleStatus`，故无人兜底。
>
> 实测复现（本机 Release，合成 64×64 结构化图，逐节点 `init()` → 喂图 → `execute()`）：**修复前 63 个可脱机执行节点 = 29 通过 / 34 失败**。加入"成功默认值"后 **52 通过 / 11 失败** ⇒ **23 个节点是纯契约受害者**（其余 11 个是真缺资源，见下）。
>
> 修复（契约翻转，非逐节点打补丁）：`HalconNode::process()` 每轮 `run()` 前先预置 `moduleStatus=true`，失败**必须由 `run()` 显式写 false**；同时补上另一半——声明需要输入图像的节点本轮无可用图像时**必须显式失败**，否则"成功默认值"会让 `run()` 里 `if (!initialized) return;` 的算子在空输入下显示绿灯。判据取**端口类型**而非注册分类（`requiresInputImage()` 扫 `PortDataType::Image` 输入端口），因为分类由拖放/注册路径决定、端口类型才是节点自身的声明。
>
> **本次修复引入过一次真实回归并已定位**：空输入守卫首版按端口类型一刀切，`IntegrationTest::testScriptNodeReportsSuccess` 与 `testConditionalBranchSkipClearsStaleOutput` 稳定失败（两轮各 2 FAIL，非偶发）。根因：`ConditionalNode::init()` 为兼容旧项目会调用 `HalconNode::init()`，因而继承到 Image 输入端口，但其判定逻辑并不消费图像；`ScriptNode` 的图像同理可选。改法是把判据做成可覆写虚函数，两处 override 返回 `false`（`include/ConditionalNode.h`、`include/ScriptNode.h`）。
>
> 剩余 11 个失败节点**不是缺陷**，逐个读码确认均为"缺外部资源"，已在门禁里进排除表并写明理由：`ColorConversionNode`（覆写了 `process()`，默认 `RGB_TO_GRAY` 遇单通道灰度输入按设计返回失败）、`AnomalyDetectNode`（未教学即显式写 `moduleStatus=false`）、`OpencvTrainClassifierNode` / `OpencvClassifyNode` / `DnnInferNode` / `DnnDetectNode` / `DnnSegmentNode` / `OpencvCalibNode`（缺数据集 / 模型文件 / 标定数据）、`OpencvQrNode`、`ZxingBarcodeNode`、`TesseractOcrNode`（合成图无码无字，"解出结果为空即失败"是这些节点的既定语义）。
>
> 门禁（一次性钉死全族，不再依赖人工点名）：`tests/node_execute_smoke_test.cpp` → CTest `NodeExecuteSmokeTest`，`initTestCase()` 先 `registerAllNodes()`（**独立测试进程不注册则注册表为空，会"零节点静默通过"**，故另设 `registryIsNotEmpty` 断言注册数 > 60），随后三条断言（**同日已扩为五条口径，见下一段 S-1**）：默认参数 + 合成图必须成功（受检数 ≥ 50）、要求输入图像的节点缺图必须失败（受检数 ≥ 40）、缺资源的可配置节点必须显式失败。**该门禁的变红能力已自证**：临时把预置值改回 `false` 重跑，`NodeExecuteSmokeTest` 确实 `***Failed`，随后恢复。

**2026-09-25 同日追加：S-1 —— 上一段"成功默认值"自引入的第二类静默绿灯（已在基类收口，非逐节点补丁）**

> 机制：翻转后的契约把"未显式写 `false`"等同于成功，而本族 `run()` 里大量失败/异常路径只做 `m_outputImage.Clear(); return;`，或 `catch (const std::exception &) { m_outputImage.Clear(); }`，**从不写 `moduleStatus=false`**（`cv::Exception` 派生自 `std::exception`，故 OpenCV/Halcon 抛错同样落入"判成功"）。后果分两种：末端节点（显示/写文件）拿到空图而流程显示绿灯；中间节点则把红灯甩给下一个图像消费者，下游报"缺输入图像"，与真实原因不符。
>
> 静态扫描（**可复现口径已落进工具**：`python tools/scan_silent_green_paths.py`，2026-09-25 输出）：范围 `src/*.cpp`；**P1「clear-then-return」44 个文件 / 47 处**、**P2「catch 体内只 `Clear()`、体内不写 `moduleStatus`」44 个文件 / 47 处**（两组只有 15 个文件重叠 ⇒ **并集 73 个文件**），**并集里 41 个文件全仓不写 `moduleStatus=false`**。守卫因此不能靠"逐节点补写位"，只能落在基类。
> **本段上一版的数字口径已撤回**：原文写成"44 个文件命中、合计 47 处 clear+return、12 处 catch 只 Clear，其中 41 个文件不写 false"——把 P2 的数（12 是另一条更窄正则的结果）和并集分母上的 41 挂到了 P1 的 44 上，读者按 44 复算必然对不上（外部复核分别复算出 catch-only-Clear 12／22、不写位文件 13／20，差的全是分母与正则范围，现象无分歧）。以后本节数字只引该脚本输出。另：`src/ImageDivNode.cpp:47`～`:49` 在 `qFuzzyIsNull(v)` 分支**确实写了** `false`，"ImageDivNode 全文件不写"这一说（外部报告原话）不成立。
>
> 实测复现（修复前，加严断言先落地）：`FAIL! : NodeExecuteSmokeTest::defaultParamsWithImageSucceed() 'emptyOutput.isEmpty()' returned FALSE. (以下算子判成功但 0 号图像输出端口为空（静默绿灯）：OpencvAngleNode)`；尺寸不符探针逐节点输出 `MM ImageSubNode ok=1 promises=1 empty=1`、`MM ImageDivNode ok=1 promises=1 empty=1`（同批还记录 `ConditionNode ok=1 promises=1 empty=0`、`FormulaNode promises=0`，用于圈定不误伤"端口本就非 Image"的节点）。
>
> 修复（三条）：① **基类产出守卫**——`HalconNode::process()` 在 `run()` 之后、发布输出之前加一段"`requiresImageOutput()` 且无产出 ⇒ 写 `moduleStatus=false`、清空全部输出、`return false`"；判据与 `requiresInputImage()` 对称（只有"既吃图、0 号输出端口又声明为 `PortDataType::Image`"的节点才要求产出），故只产数值/区域的节点不受影响，确需豁免的节点覆写 `requiresImageOutput()` 返回 `false`。② `OpencvAngleNode::run()` 成功分支补 `m_outputImage = m_inputImage`（它经 `HalconNode::init()` 对外承诺了"输出图像"端口，测量值在端口 1/2，不透传就是给下游空图）。③ **自查发现、审核未提**：`CameraIoNode` 同样调用 `HalconNode::init()` 却不消费图像，上一轮的空输入守卫会让它"未接图像时必判失败"；因它在设备排除表里、门禁从未覆盖到 ⇒ 现 `include/CameraIoNode.h:20` 覆写 `requiresInputImage()` 返回 `false`。
>
> **未采用的方案**：逐个给 41 个文件补写 `moduleStatus=false`——覆盖不全（新算子还会再犯），而且正是上一轮"一刀切守卫引发 IntegrationTest 回归"的重演路径。
>
> 门禁升级（`NodeExecuteSmokeTest`，现为 7 个测试函数 / 六条口径）：① 由"`execute()==true`"加严为"并且 0 号 `Image` 输出端口非空"；④ 新增 `mismatchedImageSizesNeverGreen`——入选口径是**有 ≥2 个 `Image` 型输入端口**的节点（第三轮复核指出：只看"输入端口数 ≥2"会把纯数据型多输入节点算进分母，喂两张不同尺寸的图对它们不构成有意义的"尺寸不符"），实测满足者只有 `ImageSubNode` / `ImageDivNode` 两个，故阈值取 2 并**注明它已不是守卫的主要保障**；⑤ 新增 `skipListsMatchRegistry`（排除表里的 id 必须仍在注册表内，防算子改名后覆盖面静默缩水）；⑥ 新增 `outputGuardShapesBehaveAsContracted`——用三个测试内探针节点**直接钉住守卫本体**（`run()` 只 `Clear()` ⇒ 必须失败且清空输出；不覆写 `run()` 走基类默认透传 ⇒ 必须成功；覆写 `requiresImageOutput()=false` 且只 `Clear()` ⇒ 必须仍成功，豁免通道不能失效）。遍历式用例只能覆盖"恰好有真实节点走到 `Clear()` 分支"的情形，守卫被删或被改成无条件都会漏过去，故必须有这条不依赖注册表的锁。覆盖面断言从软下限升级为精确等式 `executed == eligible`，创建失败一律记入 `notCreated` 而不是静默跳过；**实质结论一律排在覆盖面之前**——本轮就发生过 `checked >= 5` 先失败中断测试函数、把同函数内已命中的 `ImageSub`/`ImageDiv` 掩盖掉。
>
> 实测（2026-09-25，本机 Release 全量重编后）：`NodeExecuteSmokeTest` **7 个测试函数全部通过**；注册表 **71** 条，① 应检 **53** / 实检 **53**，④ 应检 **2** / 实检 **2**；全量门禁 `ctest -C Release` **28/28 通过**。口径对账（免得与上面"修复前 63 个"对不上）：53 = 71 − 设备排除表 8 − 缺资源排除表 10，而"63"只减了设备那 8 项 ⇒ 63 = 53 + 10，两处一致。新断言 ⑤ 的变红能力单独自证过：临时往排除表塞一个不存在的 id，立刻 `FAIL! : ...skipListsMatchRegistry() 'stale.isEmpty()' returned FALSE. (跳过清单里有已改名/已注销的 id（覆盖面正在悄悄缩水）：NoSuchNodeId_ZZZ)`，随后删除该临时条目。
>
> **审核建议中本轮未实施的一条（W-2）**：数据型 / `Any` 输入端口空载时仍显示绿灯。事实是这批节点（`ClassifyNode`/`CounterNode`/`DelayNode`/`FilterNode`/`FormatNode`/`FormulaNode`/`ProtocolParseNode`/`RecordNode`/`SortNode`）各自覆写 `process()` 且形如 `{ run(); return true; }`——**无条件返回成功，基类两道守卫都碰不到它们**。要给它们加"成功必须有产出"，需要先逐端口定义必填/可选契约（含旧项目兼容），属独立改造，不在 P0-1 收口范围内，此处记为**未做**。
>
> ⚠️ **上段"未做"已于 2026-09-25 随阶段 A 的 A3 实施，该结论就地作废**（原文保留只作过程记录）。做法不是给九个文件各补写位，而是给图像侧两道守卫补**数据侧孪生**并让九个 `process()` 统一走同一个 helper：
> - **判据**（`include/HalconNode.h`，与 `requiresInputImage()`/`requiresImageOutput()` 同风格，取**端口类型**而非类名）：`requiredInputDataPorts()` 默认返回"全部非 `Image` 输入端口下标"、`requiresDataOutput()` 默认"存在非 `Image` 输出端口"。两者都是 `virtual`，即"逐端口必填/可选"的声明位。
> - **执行**（`HalconNode::processDataOutputs()`，`src/HalconNode.cpp:503`）：清空图像与全部输出端口（P1/P2 的数据侧）→ **必填数据端口无可用值即失败**（写 `moduleStatus=false` + 再清一次输出 + `return false`）→ 预置 `moduleStatus=true`（P0-1 同一条契约）→ `run()` → `run()` 显式写 false 则失败 → **承诺产数据却全部端口为空则失败**（S-1 的数据侧孪生）→ `catch(...)` 同样写位并清空。"可用值"= `DataObject` 存在且 `getData()` 有效非空；因为 `setHImage()` 有意不写 `m_data`（`src/DataObject.cpp:50`～`:64`），**误接到数据端口上的图像对象算空载**——这也正是断言① 的喂图冒烟无法判定数据节点的原因。
> - **唯一的非默认口径**：`FormulaNode::requiredInputDataPorts()` = 表达式里作为独立标识符出现的 `p0`~`p3`（`\bp([0-3])\b`，函数名 `pow` 不误判）。旧项目兼容出口就是它：常量公式（`2 * 3`）必填集为空 ⇒ 空载仍合法；而"引用了却没接线"的操作数从**静默绿灯**（旧实现 `{ run(); return true; }`，且 `run()` 走到底只会 `evaluate()` 失败、清空输出，即"成功且零产出"）变为**判失败**。
> - **一处显式豁免**：`DelayNode` 两条判据都让开（`include/DelayNode.h`）。理由：延时的职责是时间门控（循环体 / 暂停 / 节拍），`数据输入` 只是可选透传，空载时 `run()` 明确清空输出 ⇒ 不产任何"参数派生假结果"，与 `LoopNode` 的"透传即视为成功（E5）"同族。**证据**：不给豁免时全量门禁实测 **4 个套件 / 14 个测试函数变红**（SoakTest 1、NodeGroupTest 2、FlowSnippetTest 1、IntegrationTest 的 Loop/延时系列 10；前三个套件已 grep 核实跑的都是纯延时流程）。豁免由 `delayStaysGreenOnEmptyLoad` 反向钉住（同时钉"空载不得凭空造数据对象"），防止豁免被顺手删掉、也防止判据被改成无条件失败。`SubFlowNode` 同样命中端口口径但列入豁免表：它的 `process()` 是 `return invoke()`，成败由子流程执行结果决定，`Any` 端口是调用接口而非数据输入。
> - **豁免没有靠改弱判据实现**：口径仍然按端口类型算，命中就多收，命中后又不要必填的节点必须在测试里进 `kW2ExemptDataOnlyNodes` 并**写理由**；`skipListsMatchRegistry` 会校验专测清单与豁免表的 id 都还在注册表里，`defaultParamsWithImageSucceed` 会校验"实际命中的数据集合 == 专测清单"（多收/漏收都变红）。
> - **门禁扩到 12 个测试函数**（`ctest` 里 14 行含 `initTestCase`/`cleanupTestCase`；上一段记的"7 个测试函数"是 P0-1/S-1 那轮的时点数）。本轮新增 5 条：`dataNodesFailOnEmptyLoad`（八个受约束节点空载 ⇒ `execute()==false` **且** `moduleStatus==false` **且** 每个输出端口为空）、`dataNodesSucceedWithTypedInput`（反向：按端口类型喂合法值必须成功且有产出）、`dataGuardShapesBehaveAsContracted`（**不依赖注册表**的四形态探针锁：空载必红 / 有输入零产出必红 / 正常产出必绿 / `requiresDataOutput()=false` 的豁免通道仍绿但空载仍红）、`formulaRequiredPortsFollowExpression`（口径段直接比对 `requiredInputDataPorts()` 集合 + 行为段比对 `execute()`）、`delayStaysGreenOnEmptyLoad`。覆盖面数字由门禁自己打印成一行 `W2-SCOPE`，本轮实测 **`regs=71 eligible=53 dataOnly=8 executed=45 exempt=2`**，复现命令 `ctest -C Release -R NodeExecuteSmokeTest -V`（任何人按同一条命令可复核，不必手数）。
> - **改坏自证（三次独立构建，每次只坏一处，互不混淆）**：① 把 `processDataOutputs()` 的空载守卫短路 ⇒ `dataNodesFailOnEmptyLoad` 红并点名 6 个节点（`ClassifyNode, CounterNode, FilterNode, FormatNode, RecordNode, SortNode`）、`dataGuardShapesBehaveAsContracted` 红，**`ProtocolParseNode`/`FormulaNode` 不在其中**——它们空载时 `run()` 零产出，被**产出守卫**兜住 ⇒ 这条自证顺带量出"两道判据各自兜住哪些节点"，二者不冗余；② 注释掉 `DelayNode` 的两条 override ⇒ `delayStaysGreenOnEmptyLoad` 与 `defaultParamsWithImageSucceed`（点名 `DelayNode`）红；③ 把 `FormulaNode::requiredInputDataPorts()` 改成恒返回空集 ⇒ `formulaRequiredPortsFollowExpression` 的**口径段**红（逐条给出实际/期望集合）。③ 的由来是一次**假绿的自查**：只做 ① 时那四条**行为**用例仍全绿（缺操作数 → `evaluate()` 失败 → 零产出 → 被产出守卫兜成同一个 `false`），说明"行为断言"根本区分不出必填口径，于是补了直接比对集合的口径段。三处改坏全部复原（`md5sum` 与备份一致 + 全树已无 `_A3_BREAK` 标记）。
> - **契约收紧波及的两处既有测试（都是把流程改成合法，不是放宽契约）**：`tests/soak_test.cpp` 长稳流程的公式节点原用默认表达式 `p0 + p1`，而它的连线循环每对节点只建一条线 ⇒ `p1` 空载判失败、长跑只出 1 轮（`finalStats.rounds >= 10` 红），现改为单操作数表达式；`tests/integration_test.cpp::testContinuousSecondRoundClearsStaleData` 原依赖"上游不产数据也算成功"，现显式 `setStopOnFailure(false)` 并把落点记录移到 `if (!ok) return;` 之前。
> - **实测（2026-09-25 本机全量重编后）**：`ctest -C Release` **29/29 通过**（Total Test time 88.54 s），`NodeExecuteSmokeTest` 14 行全 PASS；`python tools/repo_hygiene.py` **exit=0**（515 个跟踪文件，本轮脚本改写过 9 个 `.cpp` + 3 个头文件，按字节验收过 BOM/CRLF/无裸 NUL）。
> - **本项未收口的部分（不要读成"静默绿灯已全部解决"）**：`tools/scan_silent_green_paths.py` 本轮输出与 ⑱ 记的**完全同数**（P1 44 文件/47 处、P2 44/47、重叠 15、并集 73、**并集里 41 个文件全仓不写 `moduleStatus=false`**）——那 41 个是**图像路径** `process()` 的 Clear/catch 形态，W-2 只收了**数据端口**这一条，故 A5 仍在原地。另：`requiredInputDataPorts()` 的默认口径是"非 `Image` 输入端口全必填"，只影响走 `processDataOutputs()` 的节点（图像算子不经过这里），因此本轮没有为"图像算子带数值旁路输入"这类情形改任何默认行为——若将来有节点需要，按 `DelayNode`/`FormulaNode` 的方式覆写并给理由。

**2026-09-26 追加（阶段 A 的 A5）：上面「41 个不写位」这条口径的运行期复核 —— 跟踪项，未改产线代码**

> 上一段把 A5 留在原地，前提是「那 41 个文件的失败路径对契约不可见」。本轮动手前先核了两件事：**那 41 个文件是否真的无人看守**，以及**把「静默绿灯」换成运行期量得到的口径重量一遍**。
>
> - **逐文件核对 ⇒ A5 原计划（逐文件补位）作废**：对 `python tools/scan_silent_green_paths.py --files` 输出的 41 个文件逐个 grep `^bool <Node>::process\(` ⇒ **41/41 命中 0**，而 `^void <Node>::run\(` **41/41 命中**——即**没有一个自写 `process()`（该划分现由脚本自身给出：`python tools/scan_silent_green_paths.py --files` 末段 `of those: own ::process() 0`，本轮把这条口径补进了工具，不必再手工 grep 复算）**，全部走 `HalconNode::process()`（`src/HalconNode.cpp:427`～`:501`）那条已含「空输入守卫 + 预置 `moduleStatus=true` + 失败传播 + 产出守卫 + `catch(...)` 写位」的路径。再补写一遍 `moduleStatus=false` **不改变任何运行期行为**，故不做。上一段那句「那 41 个是图像路径 `process()` 的 Clear/catch 形态」的**形态判断成立**，但由它推不出「需要逐文件补位」。
> - **更正一处我方文档错误**：路线图 ㉓ 末条、㉔「顺带核出」②、以及本计划 A5 行动作都写着扫描脚本的 `--files` 参数**不存在**。**不成立**——`tools/scan_silent_green_paths.py` 里 `show_files = "--files" in sys.argv[1:]`（`:79`）与其打印段（`:95`～`:98`）都在，本轮实跑 `--files` 得到上面那 41 行清单。属文档漂移，不是脚本缺陷。
> - **运行期口径重量（新立，取代「文件级形状计数」作为跟踪指标）**：静默绿灯的实质不是「文件里没写位」，而是**「`execute()` 判成功，但端口上什么都没有」**。据此让门禁自量自证（`tests/node_execute_smoke_test.cpp::baseProcessNodesWithDataPortsAreMeasured`：注册表逐个建节点、喂一张合成图、单次 `execute()`，只统计声明了非 `Image` 输出端口的节点）。本轮实测 **`candidates=24 imageGuarded=22 exposedDataPorts=1 exposedAllPorts=1`**、`A5-BARE ids=[SubFlowNode]`、`A5-UNGUARDED ids=[DelayNode, SubFlowNode]`。复现：`ctest -C Release -R "NodeExecuteSmokeTest|SubFlowTest" -V`。
>   - **`imageGuarded=22` 只能当结构参考**：它数的是「`requiresImageOutput()` 为 true」，而该判据**只在基类 `process()` 路径上生效**。本轮 grep 到 17 处 `::process()` 覆写，其中 9 处转 `processDataOutputs()`，其余 7 处（`ColorConversionNode`、`HalconImageSourceNode`、`MvsImageSourceNode`、`ImageReadNode`、`ReceiveDataNode`、`SendDataNode`、`SubFlowNode`）自己写返回值。因此结论取**行为侧那一个数**：`exposedAllPorts=1`。
>   - **唯一命中的 `SubFlowNode` 命中的是设计期那一支**：无执行器上下文时 `invoke()` 清空输出并返回 true（`src/SubFlowNode.cpp:26`～`:37`，节点自检路径）⇒ 成功 + 全端口空，与 `DelayNode` 的 E5 透传豁免同族。已落成**带理由的白名单** `kZeroOutputOnSuccessExemptions`（`tests/node_execute_smoke_test.cpp`），白名单之外的新节点命中即判红。**改坏自证**：临时清空该表 ⇒ `baseProcessNodesWithDataPortsAreMeasured` 判红并点名 `SubFlowNode`（`Totals: 2 passed, 1 failed`），复原后转绿。
>   - **有执行器那一侧此前无人量过**：上一版测试注释按 C++ 语义推定「成败由子流程结果决定」，属**归因强于证据**，已改写。新增 `SubFlowTest::testUnfedSubFlowCallerStaysGreenAndEmpty`（调用点故意不接上游、成员为两条延时），实测 **`caller=green member1=green member2=green outputEmpty=true errors=0`** ⇒ 现语义是「绿灯 + 空输出」，**不凭空造数据**。**两次反向改坏自证**（各命中一条断言，同套件另 8 条不受影响）：① 把出口 `out` 换成新建空对象 ⇒ 「不得凭空造数据」判红；② 把「出口无数据」改成 `failWith` 判失败 ⇒ 「豁免不得被改成空载必红」判红（`caller=red`、`errors=2`）。两次均复原，`src/FlowExecutor.cpp` `md5sum` 与备份一致（`e1cbe063216495af2737f71ca9a34402`）。**「子流程出口无数据」是否应判失败属产品语义，本轮未拍板 ⇒ 记为开放项 O-6**，本项只钉现状、不改行为。
> - **顺带修掉一条门禁不确定性（本轮唯一的代码改动，改的是测试）**：从**仓库根目录**直跑冒烟 exe 时 `unconfiguredResourceNodesFail` 变红并点名 `TesseractOcrNode`。根因不在断言：该节点 `tessdataPath` 留空时按**相对 cwd** 兜底到 `thirdparty/tesseract/tessdata`（`src/TesseractOcrNode.cpp:60`～`:69`），仓库根有该目录、`build/` 没有 ⇒ 「缺外部资源」的前提随工作目录变；ctest（cwd=build）一直绿，但这条断言是**碰巧依赖工作目录**才成立。修法：`kParamTweaks` 把该节点 `tessdataPath` 钉成不存在的目录，两种 cwd 下同一口径。复测：仓库根直跑 `Totals: 15 passed, 0 failed`、全量 `ctest -C Release` **29/29 通过（`Total Test time (real) = 99.26 sec`）**。
> - **A5 的跟踪指标从此改为 `A5-SCOPE` 里的 `exposedAllPorts`（白名单外必须为 0），不再引脚本的 41／73**：后者是源码形状统计，基类集中写位永远不会让它下降，拿它画「下降曲线」等于要求代码变差。`tools/scan_silent_green_paths.py` 继续 informational（exit 恒 0），不进 CI。

**测试覆盖现状（2026-09-24 实测更正，2026-09-25 同日多次复跑；最新一次为 A4 之后）**：`tests/integration_test.cpp` 现为 **50 个测试函数**（口径即三条计数命令，本轮实测值：`grep -c "^    void test"` = **50**、`grep -cE "^void IntegrationTest::test"` = **50**、`grep -c "QTest::newRow"` = **0** 即无数据驱动行），E1–E6 各自有真跑用例（真建流程、真连线、真跑两轮以上、断言跨轮取值）。**上一轮记的是 44**（A1-④ 之后的数），44→50 的 **+6 全部来自 A4**：E1 三条（还原早于广播 / 广播里抛非标准异常 / 还原点之前抛出）、E2 一条（跳过与失败撤销可复用标记）、E3 一条（建图时建立归属）、E6 一条（默认节拍不拉住两轮间隔）。**残留缺口的覆盖状态随之改变**：此前写的"上表「残留」小段列出的缺口均无对应用例覆盖，故不能以'用例全绿'反推'该项已修复'"在 A4 之前成立，现在只对**未被 A4 收口**的部分成立——E1/E2/E3 的残留已各有专门用例并逐条改坏自证（见 §3.21 各行"A4 收口"段），E6 有断言但**默认节拍策略仍未拍板（O-1）**；三行末尾"本轮未收口的边界"里列的缺口依旧无用例，那条推论对这些缺口继续有效。另有 `testEndToEndPipelineSmoke`、S1 的"连续运行中反复增删节点"压测段与图快照/墓碑契约用例。原文"5 个 CTest 通过 / `integration_test.cpp:45` 只校验节点创建与空输入 / 不能覆盖 E1–E6"**已不成立**。全量门禁：**2026-09-25（A1 之后）本机 Release 全量重编后复跑，`ctest --test-dir build -C Release` 29/29 通过**（上一段记的 28 是 A1 之前；本次 +1 来自 A1-① 新增的 `FactoryPasswordGuardTest`，而 `integration_test.cpp` 的 43→44 来自 A1-④ 新增用例，两处是不同维度的计数——套件数 ≠ 测试函数数）；**A4 之后（2026-09-26 晨，全部改坏还原 → 整树重编 → 全跑）复跑仍为 29/29 通过，`Total Test time (real) = 102.45 sec`**（套件数未变：A4 只加测试函数、没加套件）。
>
> **同日修掉的一条门禁可用性缺陷（W-A，2026-09-25 第四轮更正根因）**：此前 **ctest 拿不到任何 QtTest 明细**。真因不在本仓代码也不在链接库里：**QtTest 的 plain-text(txt) 日志通道在 stdout 被重定向时（ctest 的管道、`> file`、IDE 输出窗都算）整条关闭**，给子进程设 `QT_ASSUME_STDERR_HAS_CONSOLE=1`（或 `QT_FORCE_STDERR_LOGGING=1`）即恢复；同一 exe 的 `-functions`、`-o -,junitxml`、`-o -,tap`、`-o <文件>,txt` 四条通道在重定向下一直正常，`-v1` 全量仍 0 行（排除"缓冲没 flush"）。⚠️ 本节首版把它归因为"链接 `vfp_core` 的测试 exe 会丢掉 stdout"，并拿 `opencv_nodes_test`（29 行）作对照——**两条都不成立**：`-functions` 走的是同一进程的完整 DLL 静态初始化却有输出；且 `ctest` 里 28 个套件的 target **全部**链接 `vfp_core`（逐个核过 `target_link_libraries`），根本没有"未链接 vfp_core 的 QtTest"可作对照，而 `opencv_nodes_test` 不是 QtTest（自写 main + `std::printf`），只能证明"非 QtTest 程序可以打印"。Qt 内部按什么句柄判定"有没有控制台"未取源码证据，**本节只陈述"能复现、能开关"**。两个 exe 的 PE 子系统都是 console(3)，与 `WIN32_EXECUTABLE` 无关。影响是硬伤：28 套件里任何一条断言变红，`ctest --output-on-failure` 只能显示 `Test Failed`，而文档里引用的 `FAIL! ...` 明细全部来自"手工 `-o <文件>,txt` 落盘"，别人按 CTest 复现不到、CI 上更无从定位。改法：28 条 `add_test` 统一换成 `vfp_add_qtest(NAME … TARGET …)`（`CMakeLists.txt`），经 `tools/run_qtest.cmake` 用 cmake.exe 的 stdout 把结果文件内容转发给 ctest——**明细从此不依赖调用方环境**。包装带三道闸：**没有 `Totals:` 行判失败**（进程启动即崩 / 被外部杀掉时退出码不一定非 0，"没跑完"不能算通过）、**`N failed > 0` 判失败**、**`Totals: 0 passed` 判失败**（整套件被 skip 或用例名失配时也是安静绿灯，与前两道同源）。**负向自证**：① 临时在某用例塞 `QFAIL`，`ctest -R NodeExecuteSmokeTest --output-on-failure` 完整打印 `PASS/QDEBUG/FAIL!/Totals` 明细并判该套件 Failed；② 用伪 exe 造 green / `0 passed` / `1 failed` / 无 `Totals` 四种结果，逐一验得退出码 0/1/1/1 与对应报错文案（第四轮补，脚本口径见 `tools/run_qtest.cmake` 文件头）。（本段三处"28"是该轮时点数，A1-① 之后为 **29**；新增的 `FactoryPasswordGuardTest` 同样走 `vfp_add_qtest` 且链接 `vfp_core`（`CMakeLists.txt:1030`～`:1032`），故本段两条全称结论——"全部链接 vfp_core""全部经包装注册"——重核后仍成立。）
>
> **2026-09-26 追加（阶段 A 的 A6）：判红必须留档，且上面那几道闸要有常驻自检**。上一段的对照探针当时是**一次性手工**跑的，脚本没进仓就随 `build/` 清空失传；而本包装的 `${VFP_LOG}` 又在每次运行开头被 `file(REMOVE)` 覆盖——两件事叠起来，「红一次、重跑就绿」的偶发失败**当场没有证据**，A6 的 IntegrationTest 观察项正是这一形态，不补这一环它永远无法结案。本轮改两件事：
> ① `tools/run_qtest.cmake` 增加 `vfp_preserve_failure()`（函数体 `:78`～`:87`），四个判红分支各调一次（`:90`／`:95`／`:102`／`:106`），判红之前把结果文件另存成 `build/Testing/<套件>.failed-<UTC 时间戳>.txt`（时间戳命名 ⇒ 多次判红互不覆盖；只增不删）。**结果文件从「覆盖一次就没痕迹」变成「每次判红都留一份」**，观察项才谈得上结案。
> ② 把上面那次一次性探针落成常驻可执行定义 **`tools/selftest_run_qtest_gates.py`**：用伪 QtTest 造**六种**结果形态——`green`／`failed`（Totals 有 failed 且 rc=1）／`failedrc0`（rc=0 但 Totals 里有 failed）／`zeropassed`（全 skip）／`nototals`（有文件、没 Totals 行）／`nocfile`（什么都不写）——逐条核对**退出码 + 是否留下 `.failed-` 档 + stdout 里 `.failed-` 出现的次数**。比对只用 ASCII 片段：CMake 那句中文提示的落码随调用方代码页变，按中文匹配会把"探针本身失效"误读成"闸门失效"。本轮实测 `QTEST-GATES cases=6 mismatch=0`、脚本 **exit 0**；复现命令 `python tools/selftest_run_qtest_gates.py`。
> **它已接进 CI（不留新的死闸）**：`tools/ci.ps1` 新增「1c. QtTest result-wrapper self-test」步（`:116`～`:138`，紧随卫生步、复用同一段 Python 解释器解析结果），自检不过 ⇒ **exit 5**（新占用码，已写进该脚本文件头码表 `:15`）。
> **两条改坏自证**。**甲（分支敏感性）**：摘掉 `0 passed` 分支里那一句 `vfp_preserve_failure("0 passed")` ⇒ 六条里**只有 `zeropassed` 一条 MISMATCH**、脚本 exit 1（`QTEST-GATES cases=6 mismatch=1`），其余五条仍 OK ⇒ 证明六条各锁各的门，不是一个大开关。**乙（真实门禁端到端）**：在 `tests/node_core_test.cpp::testParamSetGet` 临时注入 `QFAIL` 后重编该目标 ⇒ `ctest -C Release -R NodeCoreTest` 判 **Failed**（`CTEST_EXIT=8`），并留下 `build/Testing/NodeCoreTest.failed-20260926T024018Z.txt`：第 4 行 `FAIL!  : NodeCoreTest::testParamSetGet() A6 break-proof: temporary failure, must be reverted`、第 17 行 `Totals: 12 passed, 1 failed, 0 skipped, 0 blacklisted, 48ms`，指针行「保留失败明细：…（QtTest 退出码非 0）」当时打在该次 ctest 的 `build/Testing/Temporary/LastTest.log`（⚠️ 该文件每次 `ctest` 都被覆盖 ⇒ **不能当可复核证据**，可复核的是那份 `.failed-` 档本身和下一条的自检脚本）。撤掉注入、按 `md5sum` 复原（`0ffaa80d8998d6c8351ad5d8f98f703e`）后重编 ⇒ 该套件 **100% passed**，且 `Testing/` 下**不再新增** `.failed-` 档（仍是那一份 02:40:18Z 的）。包装改动对全量门禁无影响：`ctest -C Release` **29/29 通过**（`Total Test time (real) = 95.14 sec`），`python tools/repo_hygiene.py` **exit=0**。
> **一处未取到的证据（不要把本段读成「CI 已端到端跑通」）**：本轮 `powershell -File tools/ci.ps1` 的**整脚本执行被会话权限闸拦下**（分类器把 `ci.ps1` 归入可能涉及提交/推送一类），因此 1c 步只验到三件事——PS 解析器 `PSParser::Tokenize` 对全文件 **0 errors**（14283 字符 / 1595 token）、该步文本确在脚本内、以及**该步实际发出的同一条命令行**（`python tools/selftest_run_qtest_gates.py --cmake <(Get-Command cmake).Source`）在 PowerShell 下逐字跑通并 exit 0。**「1c 在 `ci.ps1` 里被真正执行过一次」仍待下轮端到端复核**，在此之前该步完成度只能记作「命令行已验、宿主脚本未跑」。

**仍然成立的边界（这一段不要连同上面的更正一起划掉）**：上述全部是**合成图像 + 内存内流程**的验证，只证明执行引擎语义正确，不证明工业现场交付水平。真机（MVS 相机触发/断流、PLC 联动）、现场 7×24 长稳、以及 §7 的 AC1–AC10 **实测记录目前为 0 条**（AC 表此前连状态列都没有，2026-09-24 已补，逐条见该列）。因此路线图 §1 的结论仍应读作："功能与语义已在纸面和合成场景闭环，现场验收未开始"——而不是"已对标 VM 4.4"。

---

## 4. 非功能需求（NFR）

### 4.1 性能（NFR1.x）

| 编号 | 需求描述 | 指标 | 状态 |
|------|---------|------|------|
| NFR1.1 | 拖拽/连线等编辑操作响应 | ≤ 100 ms | ✅ |
| NFR1.2 | 软件冷启动时间 | ≤ 3 s | ✅ |
| NFR1.3 | 单算子执行耗时可观测（性能面板统计次数/总耗时/均值/最值） | 支持，>100ms 标红 | ✅ |
| NFR1.4 | 大规模流程（≥ 100 算子）编辑流畅度 | 撤销/连线无明显卡顿 | ✅（有 PerfTest） |
| NFR1.5 | 典型视觉方案单次执行耗时 | 由方案决定，需可视化定位瓶颈 | ✅ |
| NFR1.6 | 相机采集到结果输出端到端延迟 | 按需可测，提供时间戳埋点 | ⚠️ 部分 |
| NFR1.7 | 深度学习推理吞吐 | 提供 CPU/GPU 后端与批处理 | ❌ |

### 4.2 可靠性（NFR2.x）

| 编号 | 需求描述 | 状态 |
|------|---------|------|
| NFR2.1 | 相机/通讯掉线自动重连，恢复后无需人工干预 | ⚠️ 通讯已支持，相机部分 |
| NFR2.2 | 方案文件写入原子性，断电/崩溃不产生损坏文件 | ✅ 已交付（2026-09-24 复核回写，原标 `❌` 已过期）：`ProjectManager` 保存走 `QSaveFile`（先写临时文件、`commit` 成功才改名覆盖目标，`src/ProjectManager.cpp:112`～`:115`；加密/只读包与运行界面布局同口径 `:228`、`:298`）⇒ 中途断电只留临时文件，不会截断原方案 |
| NFR2.3 | 异常（算子抛错、相机断流）不导致主程序崩溃 | ⚠️ 部分；**E3 部分修复**（**2026-09-25 由"E3 已修复"降级**：执行路径上相机优先取 `ownerExecutor()`、双流程隔离由 `testTwoExecutorsIsolation` 钉住，但节点归属只在首次执行时建立，未跑过的节点其 UI/参数路径仍回落全局 `current()`，见 §3.21 E3「残留」）；算子抛错/空输入有集成用例（`testErrorHandling` 断言空输入不崩且 `moduleStatus=false`）。**未验证**：相机断流（拔线/链路中断）无法在合成场景构造，属真机验证项 |
| NFR2.4 | 7×24 连续运行无内存泄漏、无句柄泄漏 | ⚠️ 部分（2026-09-24 建立基线，2026-09-25 更正 E4 表述）：**E4 只修了延时可取消这半边**；"运行中销毁安全"仍开放——析构 `wait(5000)` 超时后照常拆解成员，`testDestroyWhileRunningIsSafe` 只覆盖线程被唤醒迅速退出的快路径（见 §3.21 E4「残留」），故**不能**据两条用例判"崩溃路径已修复"；**合成长稳基线已建立**——`docs/长稳与文档对账.md` 实测 25 秒：2313 轮 / 92.5 轮每秒 / 0 失败轮 / 句柄 Δ1 / 工作集 Δ0 MB（ctest 内置 20 秒段）。**未做**：72 小时现场长跑只有命令（`tools/soak.ps1 -Hours 72`）而**无实测记录**，跑完之前本条不得判通过 |
| NFR2.5 | 关键操作（删除流程、覆盖保存）二次确认 | ✅ |
| NFR2.6 | 崩溃自动保存现场并可恢复方案 | ✅ 已交付（2026-09-23）：`RecoveryStore`（QSaveFile 原子写）+ `tests/recovery_store_test.cpp` 12 条用例；恢复文件损坏/截断时**拒绝**冒充"可恢复"，并明确告知后清除 |

### 4.3 稳定性（NFR3.x）

| 编号 | 需求描述 | 状态 |
|------|---------|------|
| NFR3.1 | 正常退出时内存泄漏为 0（无泄漏报告） | ⚠️ 需长稳验证 |
| NFR3.2 | 异常输入（空图、超参、非法连线）有明确提示且不崩溃 | ⚠️ 部分；**E2 部分修复**（**2026-09-25 由"E2 已修复"降级**：跳过分支同时清空输出端口与节点缓存，但漏清 `m_validOutputs`，当前因"增量复用"分支已回退而不可达、属潜伏残留，见 §3.21 E2「残留」）；空输入不崩溃且有可见失败态有 `testErrorHandling` 钉住。**本轮新增（P0-1 副作用，正向）**：声明需要输入图像的节点缺图时统一**显式失败并清空输出**（`HalconNode::requiresInputImage()` + 空输入守卫），不再出现"空输入仍显示绿灯"，该行为由 `NodeExecuteSmokeTest::imageInputNodesRejectMissingImage` 遍历注册表钉住。**同日追加（S-1）**：反向的一半也已在基类收口——"吃图且对外承诺图像输出"的节点判成功却零产出时，同样**显式失败并清空全部输出**（`HalconNode::requiresImageOutput()` 产出守卫），由 `defaultParamsWithImageSucceed`（断言①）与 `mismatchedInputSizesNeverGreen`（断言④）遍历钉住。**本轮未逐项核查**：超参/非法连线在各节点的提示是否一致（无统一入口），不能据 E2 修复或空图守卫就判本条通过 |
| NFR3.3 | 多线程/多流程并发执行安全（数据库每线程独立连接） | ✅ |
| NFR3.4 | 长时间运行（≥ 72h）性能不退化 | ❌ 未验证 |

### 4.4 安全性（NFR4.x）

| 编号 | 需求描述 | 状态 |
|------|---------|------|
| NFR4.1 | 脚本执行沙箱化：语言白名单、隔离模式、环境变量剥离 | ✅（**"环境变量剥离"对 Lua 曾是空档**，2026-09-25 A1-③ 补齐，详见 FR19.2） |
| NFR4.2 | 脚本执行超时限制与用户确认 | ✅ |
| NFR4.3 | 脚本执行审计日志（JSON 落盘） | ✅ |
| NFR4.4 | 脚本安全总开关，可一键禁用所有脚本 | ✅ |
| NFR4.5 | 用户密码加盐迭代哈希存储，兼容旧哈希迁移 | ✅（哈希与迁移一直成立；**但出厂口令 `admin`/`admin` 长期可用**，"存储安全"不等于"认证安全"，2026-09-25 A1-① 收口，详见 FR18.2） |
| NFR4.6 | 操作日志审计与权限最小化 | ✅（角色矩阵成立；**取消登录仍被授予 Operator** 这条违反最小化的入口由 2026-09-25 A1-② 关闭，详见 FR18.1） |
| NFR4.7 | 方案文件与敏感配置不内嵌明文凭据 | ⚠️ 需规范 |

### 4.5 易用性（NFR5.x）

| 编号 | 需求描述 | 状态 |
|------|---------|------|
| NFR5.1 | 参数面板自动布局、分组折叠、实时预览（300ms 防抖） | ✅ |
| NFR5.2 | 新用户 30 分钟内可搭建一个完整定位+测量方案 | ⚠️ 需教程验证 |
| NFR5.3 | 所有算子提供帮助文档与参数说明（F1） | ✅ |
| NFR5.4 | 错误提示可操作（说明原因 + 给出修复建议） | ⚠️ 部分 |
| NFR5.5 | 常用操作有快捷键，且可查看快捷键表 | ⚠️ 部分 |

### 4.6 可维护性与可测试性（NFR6.x）

| 编号 | 需求描述 | 状态 |
|------|---------|------|
| NFR6.1 | 新算子注册只需在 NodeRegistry 增加条目，模板化 | ✅ |
| NFR6.2 | 算子具备自检能力与序列化测试 | ✅（NodeCoreTest / OpencvSerializationTest） |
| NFR6.3 | 核心模块有单元测试并接入 CTest | ✅（7 个 CTest） |
| NFR6.4 | 算法选型有统一策略文件（VisionIntegrationPolicy） | ✅ |
| NFR6.5 | 禁用算子保留源码并注释原因，便于回溯 | ✅ |
| NFR6.6 | CI 自动化构建与静态分析 | ⚠️ 有脚本与文档，未验证全自动化 |

### 4.7 可移植性与兼容性（NFR7.x）

| 编号 | 需求描述 | 状态 |
|------|---------|------|
| NFR7.1 | 支持方案文件向后兼容（schema 版本 + 升级迁移） | ✅ |
| NFR7.2 | 支持主流工业相机（GigE Vision / USB3 Vision） | ⚠️ 海康 + HALCON 接口 |
| NFR7.3 | 支持 Windows 10/11 主流版本 | ✅ |
| NFR7.4 | 源码与文档统一 UTF-8，避免编码损坏 | ⚠️ 历史文件已损坏，需规范 |

---

## 5. 数据需求（DR）

| 编号 | 需求描述 | 状态 |
|------|---------|------|
| DR1.1 | 方案使用 JSON 格式存储，后缀 `.vfp`，schema 版本化管理 | ✅ |
| DR1.2 | 方案需持久化：节点/连线 + 全局变量 + 相机配置 + 通讯配置 + 全局触发 + 心跳 + 标定数据 + 运行界面布局 | ✅ |
| DR1.3 | 至少支持"采集→Blob→模板匹配→直线查找→位置修正→标定"这类典型链路的持久化与复现 | ✅ |
| DR1.4 | 数据库表：报警记录、检测记录、用户、操作日志（SQLite） | ✅ |
| DR1.5 | 配方数据独立存储，支持导入导出 | ✅ |
| DR1.6 | 标定数据（齐次矩阵）命名管理并随方案保存 | ✅ |
| DR1.7 | 数据保留策略可配（默认 30 天），支持手动清理 | ✅ |
| DR1.8 | 运行界面布局持久化（runtime_interface.json） | ✅ |
| DR1.9 | 所有数据文件统一 UTF-8 编码，禁止再次出现编码损坏 | ⚠️ 待规范落地 |

---

## 6. 接口需求

| 编号 | 接口 | 说明 | 状态 |
|------|------|------|------|
| IR1 | 相机接口 | 海康 MVS SDK；HALCON `open_framegrabber` | ✅ |
| IR2 | 通讯接口 | TCP / 串口 / Modbus TCP·RTU / PLC(Modbus) | ✅ |
| IR3 | 脚本接口 | Python / Lua，经安全策略管控 | ✅ |
| IR4 | 对外控制 SDK | 供上位机加载方案、触发、取结果 | ❌ |
| IR5 | 算子插件接口 | 动态库注册自定义算子 | ❌ |
| IR6 | 数据上传接口 | MES/HTTP/数据库直写 | ❌ |

---

## 7. 验收标准

| 编号 | 场景 | 验收标准 | 实测状态（2026-09-24 复核，此前本列不存在） |
|------|------|---------|-------------------------------------------|
| AC1 | 定位引导 | 用模板匹配+位置修正，对同一样本 100 次重复，位置标准差 ≤ 0.05 px，角度标准差 ≤ 0.05° | ❌ **未实测**：需真机相机对同一样本重复 100 次的统计，仓内无该数据；合成图上的模板匹配用例只证明链路可用，不构成重复性统计 |
| AC2 | 尺寸测量 | 用卡尺/距离工具测量已知尺寸标准件，误差 ≤ ±0.02 mm（标定后） | ❌ **未实测**：需标准件 + 标定后真机；仓内只有测量算子的数值正确性单测（合成几何图），无 mm 级实物误差数据 |
| AC3 | 条码识别 | 1000 张样本（含倾斜、模糊、低对比）一维码+二维码识别率 ≥ 99.5% | ❌ **未实测**：无 1000 张样本集与识别率统计（`tests/halcon_ocr_barcode_probe.cpp` 为可用性探测，非准确率） |
| AC4 | OCR | 印刷体字符识别率 ≥ 99%；DL OCR 在复杂背景下 ≥ 95% | ❌ **未实测**：同上，无样本集准确率数据 |
| AC5 | 缺陷检测 | 标准缺陷样本集漏检率 ≤ 0.1%，过杀率 ≤ 1% | ❌ **未实测**：现有"缺陷检测/异常检测"用例是在合成图上植入已知缺陷，证明检出与定位，**不等于**标准缺陷样本集的漏检/过杀统计 |
| AC6 | 通讯 | PLC/Modbus 连续通讯 24h 无丢包，掉线后 3s 内自动重连 | ❌ **未实测**：需真实 PLC 或对手台 24h；`CommWritebackTest` 只覆盖回写逻辑，重连计时无实测记录 |
| AC7 | 稳定性 | 7×24 连续运行 72h，内存增长 ≤ 5%，无崩溃 | ❌ **未实测**：合成长稳基线已建立（见 NFR2.4：25 秒 / 2313 轮 / 句柄 Δ1 / 工作集 Δ0 MB），`tools/soak.ps1 -Hours 72` **无已完成的 72h 记录** |
| AC8 | 易用性 | 新用户参照快速入门手册，30 分钟内完成一个采图→定位→测量→输出方案 | ❌ **未实测**：需真人新用户计时，无记录 |
| AC9 | 兼容性 | 旧版本 `.vfp` 方案可被新版本正确加载并运行 | ⚠️ **部分（合成）**：运行界面旧版 JSON 兼容（`testRuntimeInterfaceLegacyCompat`）、老明文 `.vfp` 与加密信封自动识别（`SchemePackageTest::legacyJsonIsNotEnvelope`）；**缺**真实旧版本 `.vfp` 样例文件的回归（没有留存的跨版本样本文件） |
| AC10 | 安全 | 恶意脚本（越权文件访问/无限循环）被沙箱拦截或超时终止，且留下审计记录 | ⚠️ **部分（仅策略层真跑）**：`ScriptSecurityPolicyTest` 8 个测试函数覆盖决策层（语言白名单拒绝、解释器隔离参数、环境剥离危险变量、审计写结构化日志、总开关关闭即全拒等），但该套件自述"**只验证策略决策逻辑，不启动真实解释器**"。**真实进程遏制 3 用例默认不计入通过**：`testRestrictedTokenLaunch` / `testAppContainerSandboxLaunch` 需显式置 `VFP_RESTRICTED_TOKEN_TEST=1` 才跑（跨会话结果不一致，默认 `QSKIP` 并写明理由），`testScriptNodeLowIntegrityContainment` 本机无 Python 即 `QSKIP`。**仍缺**：越权文件访问被拦的端到端断言，以及**在默认门禁内真跑的超时终止断言**——"脚本到点必须被终止"这段确实存在（`testRestrictedTokenLaunch` 第 3 段），但它整体被默认 `QSKIP`，等于当前 CI 从未验证过；`testScriptNodeStopCancellable` 验的是用户主动停止，不能替代超时语义。此外未投喂真实恶意样本 |

> **口径**：本表 2026-09-24 之前**没有状态列**，因此"AC 全部通过"从未被记录过，也不成立。当前 **0 条通过现场验收**；AC1–AC8 的前置是真机与真实样本，AC9/AC10 可先补合成用例。任何对外交付材料引用本章时，必须连同本列一起引用。

---

## 8. 附录

### 8.1 需求统计

| 分类 | 条目数 |
|------|-------|
| 功能需求（FR） | 20 大类，约 130 条 |
| 非功能需求（NFR） | 7 大类，约 35 条 |
| 数据需求（DR） | 9 条 |
| 系统约束（SR） | 9 条 |
| 接口需求（IR） | 6 条 |
| 验收标准（AC） | 10 条 |

### 8.2 编码规范（防止需求文档再次损坏）

1. 所有文本资产**统一 UTF-8 无 BOM** 保存。
2. 禁止用"另存为 ANSI / GBK"的方式保存中文文档。
3. 发现乱码时**先不要保存**——保存会把替换符写死，造成永久损坏；应先确认正确编码再另存。
4. Git 提交统一 UTF-8；建议为本仓库配置 `.gitattributes` 明确文本编码。
