# VisionFlowPlatform 用户操作手册

> 版本：1.0（对应 Release 构建）
> 适用对象：方案工程师 / 调试工程师 / 现场维护人员
> 配套软件：Windows 10/11 x64、Qt 6、OpenCV 4.13（算法）、HALCON 24.11（图容器与 DeepOCR）

---

## 目录

1. [软件概述](#1-软件概述)
2. [运行环境与安装](#2-运行环境与安装)
3. [快速上手（5 分钟搭建第一个方案）](#3-快速上手5-分钟搭建第一个方案)
4. [主界面布局](#4-主界面布局)
5. [方案管理](#5-方案管理)
6. [流程搭建操作](#6-流程搭建操作)
7. [工具库全览与参数说明](#7-工具库全览与参数说明)
8. [运行控制](#8-运行控制)
9. [结果查看与数据管理](#9-结果查看与数据管理)
10. [运行界面设计器（自定义 HMI）](#10-运行界面设计器自定义-hmi)
11. [全局配置](#11-全局配置)
12. [用户与权限](#12-用户与权限)
13. [部署上线](#13-部署上线)
14. [故障排查](#14-故障排查)
15. [已知限制与路线图](#15-已知限制与路线图)
16. [快捷键速查](#16-快捷键速查)

---

## 1. 软件概述

VisionFlowPlatform 是一款对标海康 VisionMaster 4.4 的机器视觉流程开发平台，采用**流程编辑**方式搭建视觉方案：

- **算子（工具）**：以节点形式拖入画布，配置参数后按数据流连接
- **流程**：节点按连线顺序执行，图像与数据沿连线传播
- **方案**：整套流程保存为 `.vfp` 文件（JSON 格式），可随时加载复用

核心特性：

| 特性 | 说明 |
|------|------|
| 图像算法 | OpenCV（二值化、形态学、Blob、边缘、拟合、卡尺、模板匹配、缺陷、标定等） |
| 图容器 / DeepOCR | HALCON 24.11（读图、显示、HALCON DeepOCR） |
| 多流程 | 支持多标签页并行编辑多个流程 |
| 自定义 HMI | 运行界面设计器，拖拽式搭建现场操作界面 |
| 数据管理 | 全局变量、配方、结果数据库、操作日志、通信监视 |
| 用户权限 | 管理员/操作员分级登录 |

## 2. 运行环境与安装

### 2.1 硬件要求

- CPU：x64，建议 4 核以上（深度学习推理建议独显 + 6 核）
- 内存：8 GB 以上（500 万像素相机建议 16 GB）
- 存储：安装包约 500 MB，数据目录建议预留 10 GB

### 2.2 软件依赖

| 组件 | 说明 |
|------|------|
| Windows 10/11 x64 | 操作系统 |
| HALCON 24.11 | 读图、显示、DeepOCR 需要运行时与 License（详见 13.3） |
| OpenCV | 4.13.0（thirdparty/ 目录；日常图像算法与相机标定） |
| MVS（可选） | 使用海康相机时需要 MVS 运行时 |

### 2.3 安装

1. 将部署包（见 13.1）解压到目标机任意目录（建议 `C:\VisionFlowPlatform`）
2. 配置 HALCON License（见 13.3）
3. 双击 `VisionFlowPlatform.exe` 启动
4. 首次启动需**创建初始管理员账号**（用户名 + 密码≥6 位，无默认口令）

### 2.4 启动自检

启动不再拦截区域/测量自检。日常算法走 OpenCV，不依赖 HALCON 区域链路。需要时用菜单 **帮助 → HALCON 图像层自检** 检查读图/显示是否可用。方案含 **HALCON DeepOCR** 或 **Halcon图像源** 时会提示确认 HALCON 运行时。

## 3. 快速上手（5 分钟搭建第一个方案）

以"读取图像 → 二值化 → Blob 分析 → 显示"为例：

1. **新建方案**：菜单 `文件 → 新建`（或 Ctrl+N）
2. **添加读取图像算子**：左侧工具库 → `图像采集` 分组 → 双击或拖拽 **读取图像** 到画布
3. **配置参数**：选中节点 → 右侧参数面板 → 点击"浏览"选择一张测试图片
4. **添加处理算子**：拖入 **二值化**（图像处理分组）→ 参数面板设置模式与阈值
5. **添加 Blob 分析**：拖入 **Blob分析** → 设置最小面积
6. **连线**：从读取图像输出端口（右侧小圆点）按住拖到二值化输入端口 → 二值化输出连到 Blob 输入
7. **添加显示**：拖入 **显示** 算子，连接到 Blob 输出
8. **运行**：点击工具栏 **单次执行**（或 F5 软触发模式启动）
9. **查看结果**：主图像区自动显示最后一个算子图像；选中节点可查看其输出；状态栏显示状态/耗时/触发计数

> 提示：测量类算子（线段查找/圆查找/卡尺）参数面板有 **"在画布上设置搜索区域"** 按钮——点击后在图像上拖拽绘制搜索线/圆，松手自动写回坐标。

## 4. 主界面布局

```
┌──────────┬──────────────────────────┬──────────────┐
│ 工具库    │ 流程编辑区（多标签页）      │ 图像显示区    │
│（分类树 + │ 节点画布                   │（缩放/平移/  │
│  搜索框） │                          │  结果叠加）   │
├──────────┼──────────────────────────┼──────────────┤
│ 日志区    │ 状态栏：状态 | 耗时 | 触发  │ 参数面板      │
└──────────┴──────────────────────────┴──────────────┘
```

| 区域 | 功能 |
|------|------|
| 工具库 | 按分组（图像采集/图像处理/特征定位/几何测量/识别/标定/逻辑/输出/深度学习）浏览算子；顶部搜索框过滤 |
| 流程编辑区 | 拖拽/双击创建节点，画布 20px 网格，多标签页 |
| 图像显示区 | 显示算子输出图像；工具栏：适应窗口/100%/放大/缩小/十字线/保存图像；滚轮缩放、拖动平移 |
| 参数面板 | 选中节点后显示参数（"输入"标签页 = 参数配置；"输出"标签页 = 运行结果信息） |
| 日志区 | 操作与运行日志 |
| 状态栏 | 运行状态 / 本轮耗时 / 触发计数 |

所有停靠区均可拖动/浮动/关闭（视图菜单可恢复）。

**帮助**：菜单栏“帮助 → 使用手册”（或按 F1）在软件内打开本手册；手册文件位于程序目录 `docs/用户操作手册.md`，现场可直接替换更新。

## 5. 方案管理

| 操作 | 方式 |
|------|------|
| 新建方案 | 菜单 文件→新建（Ctrl+N），创建空流程 |
| 打开方案 | 菜单 文件→打开方案（Ctrl+O），选择 `.vfp` 文件 |
| 保存方案 | 菜单 文件→保存方案（Ctrl+S）；未指定路径时弹出保存对话框（自动补 `.vfp` 后缀） |
| 最近打开 | 文件菜单 → 最近打开（最多 8 条，可清空） |
| 多流程 | 流程标签页右侧"+"添加流程；标签页右键关闭 |

方案文件包含：全部流程的节点/连线/参数/布局，以及**全局配置**（全局变量、相机配置、通信配置、全局触发、心跳配置）。换机迁移时直接拷贝 `.vfp` 即可。

## 6. 流程搭建操作

### 6.1 节点操作

| 操作 | 方法 |
|------|------|
| 添加节点 | 工具库拖拽或双击 |
| 移动节点 | 拖动；连线自动跟随 |
| 选中 | 单击（高亮）；空白处单击取消 |
| 删除节点 | 右键 → 删除（连带删除关联连线） |
| 复制节点 | 右键 → 复制（含参数，副本偏移 30px） |
| 禁用/启用 | 右键 → 禁用（灰色，运行时跳过并透传） |
| 重命名 | 右键 → 重命名（影响完整名称，用于运行界面绑定） |

### 6.2 连线

- 从**输出端口**（节点右侧圆点）拖到**输入端口**（左侧圆点）
- 端口类型不匹配时拒绝连接并提示（日志区可见）
- 输入端口已连接时需先断开（右键连线 → 删除）
- 连线删除：右键连线 → 删除连线

### 6.3 撤销/重做

- Ctrl+Z 撤销、Ctrl+Y 重做（深度 50 步）
- 覆盖：节点添加/删除/复制/移动、连线添加/删除

### 6.4 画布 ROI 取点（测量类算子）

线段查找 / 圆查找 / 卡尺的参数面板 → **"在画布上设置搜索区域"**：

1. 点击按钮（需图像区已有图像）
2. 在图像上按住左键拖拽绘制搜索线（或圆）
3. 松手后坐标自动写回参数面板（右键取消）

---

## 7. 工具库全览与参数说明

> 参数面板中所有参数修改即时生效（下一轮运行采用）；带单位参数（px/mm/°）在数值框右侧显示单位。

### 7.1 图像采集

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| 读取图像 | 从本地文件读图（单张/多张/目录） | 路径（浏览按钮）、自动切换、Mono8 |
| MVS图像源 | 海康相机采集 | 相机选择、触发模式（自由运行/软触发/硬触发）、曝光/增益/帧率/像素格式（由相机配置界面提供） |
| Halcon图像源 | HALCON 接口相机/模拟 | 注意：当前版本为模拟实现（生成测试图），真实采集需硬件联调 |

### 7.2 图像处理

工具库名称即功能名。图像算法均为 OpenCV；HALCON 只出现在「读取图像 / Halcon图像源 / HALCON DeepOCR」。

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| 二值化 | 灰度区间或 OTSU 二值化 | 模式（固定阈值/OTSU）、下限 |
| 自适应阈值 | 局部均值差分二值化 | 提取方向、邻域块大小、灰度偏移 C |
| 模糊 | 高斯模糊 | 核大小（3/5/7/9/11） |
| 中值滤波 | 去椒盐噪声 | 半径、掩码形状 |
| 锐化 | 增强边缘 | 强度、模板尺寸 |
| 形态学 | 腐蚀/膨胀/开/闭 | 操作、核大小、迭代次数 |
| 图像旋转 | 绕中心旋转 | 角度（逆时针）、背景填充 |
| 图像平移/缩放/镜像 | 几何变换 | 偏移/缩放因子/插值方式 |
| 直方图均衡/对比度拉伸/灰度拉伸 | 增强 | （无参数算子） |
| 图像运算 | 图像加/减/乘/除 | 运算类型、运算对象（常数/图像）、常数 |
| ROI裁剪 | 按矩形裁剪 | 行/列/宽/高（自动边界钳制） |
| 类型转换 | 像素类型转换 | 目标类型 |
| 颜色转换 | 色彩空间转换 | RGB↔灰度/HSV/HSL（自动拆分/合并通道） |
| 灰度统计 | 最小/最大/平均灰度 | 输出对应数值端口 |

### 7.3 工具命名说明

旧方案文件里可能仍写着「OpenCV二值化」等名称，软件会按别名加载，画布上可继续用原节点名。新建时请用工具库当前名称。不要在同一流程里寻找已下架的 HALCON「阈值 / 卡尺 / 模板匹配」双轨工具。

### 7.4 特征定位

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| Blob分析 | 连通域提取与面积过滤 | 最小/最大面积；输出区域与区域数 |
| 边缘检测 | Canny 边缘 | 低/高阈值、高斯核 |
| 模板匹配 | 灰度相关定位（支持小角度搜索） | 模板路径、从图像训练、方法、最低分数 |
| 缺陷检测 | 黄金模板差影 | 模板、阈值、形态学、最小面积 |
| 角点检测 | 角点提取 | 类型、阈值 |
| 相机标定 | 棋盘格内参标定 | 内角数、方格尺寸、多帧采集 |

### 7.5 几何测量

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| 卡尺测量 | 直线卡尺多边缘 | 搜索起终点、半宽、步长、阈值、极性 |
| 直线拟合 | 轮廓直线拟合 | 最小点数、最大轮廓数 |
| 圆拟合 | 轮廓圆拟合 | 最小点数、最大轮廓数 |
| 两点距离/点线距离/点圆距离 | 几何距离 | 相关点坐标 |
| 位置修正 | 根据定位点计算位姿（仿射） | 参考点/目标点，或流程 Fixture |
| 坐标系变换 | 位姿矩阵变换 | 变换类型或 Fixture 名 |
| N点标定 | 多点标定（像素↔物理），可写入 Fixture | 点对 |
| 手眼标定 | 机器人手眼关系 | **当前为 2D 刚体拟合，非完整手眼标定** |
| 距离 | 两点像素距离 | 两点坐标 |

### 7.6 识别

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| HALCON DeepOCR | HALCON 官方 OCR（本软件唯一 HALCON 算法） | 模型路径、语言 |
| TesseractOCR | 开源字符识别 | 语言（eng/chi_sim）、tessdata 目录 |
| ZXing条码解码 | 一维条码（Code128/EAN/UPC 等） | 自动识别格式 |
| 二维码解码 | QR 码 | 解码内容；小图自动放大重试 |

### 7.7 逻辑与数据

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| 条件判断 | 按条件选择 TRUE/FALSE 分支（引擎按分支激活下游） | 条件值/输入数据；TRUE/FALSE 输出端口 |
| 循环 | 流程循环 | **注：占位节点，循环执行未实现** |
| 公式计算 | 表达式求值（p0-p3 端口 + 10 个内置函数） | 表达式文本 |
| 脚本执行 | Python/Lua 子进程脚本 | 语言、脚本文本 |
| 格式化 | JSON 模板格式化（{fieldName} 占位符） | 模板、输出后缀 |
| 数据筛选/数据分类/数据排序 | 数值列表处理 | 比较符/阈值等 |
| 条件计数 | 计数 | 条件 |
| 数据记录 | 结果写入数据库 | 字段映射 |
| 延时 | 流程延时 | 毫秒 |

### 7.8 通信

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| 接收数据 | 接收通信事件数据并输出 | 设备名、事件 |
| 发送数据 | 向通信设备发送数据 | 设备名、数据、后缀 |
| 协议解析 | 解析接收帧 | 协议规则 |
| TCP 通信节点 / 串口通信节点 | 网络/串口收发（配置界面管理） | 设备配置在"通信管理"界面 |

### 7.9 输出

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| 显示 | 透传图像并标记显示意图 | 目标窗口（主窗口/窗口1-4） |
| 写入文件 | 保存图像到文件 | 路径（浏览按钮）、文件名规则 |

### 7.10 深度学习

| 工具 | 用途 | 关键参数 |
|------|------|---------|
| 分类器训练 | 从标注目录训练分类器 | 样本目录、模型输出路径 |
| 分类推理 | 使用训练结果分类 | 模型路径、类别名 |
| ONNX深度学习推理 | ONNX 模型推理 | 模型路径、类别文件 classes.txt |

## 8. 运行控制

### 8.1 运行模式

工具栏"流程模式"下拉框：

| 模式 | 行为 |
|------|------|
| 软触发 | 点击"开始执行"（F5）运行一轮后停止；"单次执行"按钮等效 |
| 连续 | 点击"开始执行"后循环运行，直到点击"停止"（Shift+F5） |
| 硬触发 | 等待相机硬件触发信号，每触发一次运行一轮（需相机支持硬触发） |

### 8.2 单步执行

工具栏"单步"按钮：逐节点运行，每执行一个算子后暂停；再次点击推进下一个节点。配合节点颜色（绿=成功、红=失败、灰=禁用）排查流程。

### 8.3 状态栏

- **状态**：空闲/运行中/已停止/错误
- **耗时**：最近一轮完整流程耗时（ms）
- **触发**：累计触发轮数

### 8.4 编辑锁定

运行期间画布自动锁定（节点不可移动/编辑），停止后解锁。

## 9. 结果查看与数据管理

| 功能 | 入口 | 说明 |
|------|------|------|
| 图像结果 | 主图像区 | 下拉框选择算子输出 > 画布选中 > 自动最后节点；测量结果（线/圆/匹配点/卡尺边缘点）**叠加绘制在图像上** |
| 全局变量 | 菜单 系统→全局变量 | 增删改全局变量（数值/字符串/数组等） |
| 通信监视 | 菜单 通信→通信监视 | 实时查看设备收发字节/状态 |
| 结果查询 | 菜单 数据→结果查询 | 按时间/流程/结果查询数据库记录，导出 CSV |
| 操作日志 | 日志停靠区 + 菜单 数据→操作日志 | 操作与运行日志 |
| 配方管理 | 菜单 系统→配方 | 保存/加载节点参数集（按模块 ID 恢复参数） |

## 10. 运行界面设计器（自定义 HMI）

1. 菜单 视图→运行界面设计器
2. 画布拖入控件：图像显示（绑定**任意节点**输出，中间节点也可实时显示）、数值显示（绑定全局变量/节点结果）、文本标签、状态灯、按钮（触发流程动作）
3. 保存布局后切换"运行模式"（视图→运行模式），呈现现场操作界面
4. 运行界面支持多图像控件（绑定不同节点），实现多窗口显示
5. 运行界面布局随方案文件（.vfp）一起保存/恢复（换机迁移无需单独拷贝）
6. 节点参数面板"输出"页可勾选"保存到全局变量 xxx"，运行后该节点输出自动写入全局变量（运行界面绑定即实时反映真实结果）

## 11. 全局配置

| 配置 | 入口 | 说明 |
|------|------|------|
| 相机管理 | 菜单 系统→相机配置 | 枚举相机、绑定设备、曝光/增益/帧率/像素格式/触发参数 |
| 通信管理 | 菜单 通信→设备管理 | 四页签：设备（TCP/串口/Modbus 增删改）、接收事件、发送事件、心跳 |
| 全局触发 | 菜单 系统→全局触发 | 字符串/事件→流程触发映射 |
| 心跳 | 通信管理→心跳页签 | 心跳使能/间隔/内容 |
| 配方 | 菜单 系统→配方 | 配方增删改/导入导出 |
| 用户管理 | 菜单 系统→用户管理 | 用户增删改、角色（Admin/Operator） |

> 以上配置随方案文件（.vfp）一起保存与恢复。

## 12. 用户与权限

- 首次启动创建初始管理员（无默认口令）
- 登录失败记录操作日志；数据库不可用时拒绝登录
- Admin：全部功能；Operator：运行控制与查看（部分管理菜单禁用）

## 13. 部署上线

### 13.1 部署包制作

仓库根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File tools\deploy.ps1
```

自动完成：拷贝 exe → windeployqt（Qt 运行时）→ HALCON DLL + license → MVS DLL → OpenCV DLL → 创建 data/logs/schemes 目录 → 生成部署说明.txt。

### 13.2 目标机配置

1. 解压部署包，建议路径 `C:\VisionFlowPlatform`
2. 配置 HALCON License（见下）
3. 首次运行创建管理员
4. 拷贝 `.vfp` 方案到 schemes 目录（可选）

### 13.3 HALCON License

- 读图、图像显示、**HALCON DeepOCR** 需要合法 HALCON Runtime（或开发授权）
- 二值化、匹配、卡尺、缺陷、标定等日常算法走 OpenCV，不依赖 HALCON 区域/测量模块
- 帮助菜单「HALCON 图像层自检」只检查读图/显示，不作为算法可用性标准

### 13.4 上线前验证清单

- [ ] 目标机打开方案后读图、显示正常；若使用 DeepOCR，识别结果正确
- [ ] 相机联调：MVS 枚举/采集/触发模式真机验证
- [ ] 方案连续运行 24h 无崩溃、结果稳定
- [ ] 双相机/多流程压测

## 14. 故障排查

| 现象 | 排查 |
|------|------|
| 加载方案提示「HALCON 运行时」 | 方案含 DeepOCR 或 Halcon图像源，确认本机 HALCON 可用于读图/OCR |
| 找不到「阈值 / OpenCV二值化」 | 工具库现名是 **二值化**；旧方案仍按别名加载 |
| Blob 无区域 | 二值化阈值、最小面积、输入是否为二值图 |
| 相机不出图 | MVS 客户端能否枚举 → 相机配置界面参数 → 触发模式（硬触发需外部信号） |
| 通信不生效 | 通信管理设备状态、监视窗口看收发、发送节点设备名是否匹配 |
| 崩溃 | logs/crash.log + logs/crash.dmp（minidump，用 VS/WinDbg 分析） |
| 想恢复误删 | Ctrl+Z 撤销（最多 50 步） |

## 15. 已知限制与路线图

### 15.1 引擎分工

- **算法**：OpenCV（及 Tesseract / ZXing / ONNX）
- **图容器与显示**：HALCON HImage
- **HALCON 算法**：仅 **HALCON DeepOCR**
- 工具库不再提供 HALCON 阈值/区域/测量/形状匹配；请用同名 OpenCV 工具

### 15.2 功能限制

| 项 | 状态 |
|----|------|
| 循环节点 | 占位，循环执行未实现（条件分支已支持） |
| Halcon图像源 | 模拟实现，真实采集待硬件联调 |
| 手眼标定 | 2D 刚体拟合，非完整手眼标定 |
| 运行界面绑定"窗口编号" | 显示节点窗口参数由运行界面设计器的控件绑定决定 |

## 16. 快捷键速查

| 快捷键 | 功能 |
|--------|------|
| Ctrl+N | 新建方案 |
| Ctrl+O | 打开方案 |
| Ctrl+S | 保存方案 |
| F5 | 开始执行（软触发） |
| Shift+F5 | 停止执行 |
| Ctrl+Z / Ctrl+Y | 撤销 / 重做 |
| F1 | 打开使用手册 |

> 提示：调试或现场使用时，随时按 **F1** 查看本手册。

