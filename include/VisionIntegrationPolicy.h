#pragma once

/**
 * =============================================================================
 * VisionFlowPlatform · 算法集成策略（必读）
 * =============================================================================
 *
 * 一、工业相机「取图 / 采集」与 MVS
 * ---------------------------------------------------------------------------
 * · 物理设备绑定、打开/关闭、参数读写、MVS 句柄由单例 GlobalCameraManager 管理；
 *   主菜单「方案 → 全局相机配置」。
 * · MvsImageSourceNode 绑定全局相机名后，经 getCameraHandle 取流并转为 HImage。
 * · 「读取图像」用 HALCON ReadImage；「Halcon图像源」为 HALCON 采集接口。
 *
 * 二、图容器与算法分工（当前产品约定）
 * ---------------------------------------------------------------------------
 * · 端口图像总线保持 HImage / DataObject（显示、存图、连线不改为 cv::Mat）。
 * · 日常图像算法（二值化、形态学、Blob、边缘、拟合、卡尺、模板匹配、缺陷、
 *   标定、DNN 等）一律走 OpenCV。不要再按「HALCON 正统 / OpenCV 替代」双轨描述。
 * · HALCON 算法仅保留 DeepOCR（CreateDeepOcr / ApplyDeepOcr）。
 * · 未注册的 HALCON 区域 / 测量 / 形状匹配源码可留在仓库，但不得重新挂到工具库。
 *
 * 三、基类说明
 * ---------------------------------------------------------------------------
 * · HalconNode 是 HImage 输入输出与参数面板的中间基类，不代表该算子调用 HALCON 算法。
 * · 用户可见名称不要加「OpenCV」前缀；需要 HALCON 的工具在名称或说明里写明
 *   （读取图像、Halcon图像源、HALCON DeepOCR）。
 *
 * （本文件无运行时代码，仅作文档锚点供 #include 与代码审查引用。）
 * =============================================================================
 */
